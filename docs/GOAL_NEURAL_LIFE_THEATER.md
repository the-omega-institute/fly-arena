# Goal: 生命体回放与神经剧场

> **用户确认的总目标（2026-09-19）**：用公开的真实果蝇神经连接组作为 canonical brain，让人和 AI 在其基础上修改神经参数、选择演化算法、设计数字果蝇，并把它们放入可渲染的三维竞技环境中竞争、学习和演化。每次设计都必须能回放为一条可观察、可复现、可审计的生命记录：用户能同时看到果蝇身体、感觉观测、脑区/神经元活动、动作、事件、资源变化和比赛结果，并能沿谱系解释一项修改如何影响行为。

本 Goal 的完成标准是“用户真正能使用并理解研究对象”，而不是只提供接口或一个抽象分数。第一优先级是可观看的单体觅食闭环，随后扩展到脑图编辑与预览、可选演化算法、双体竞技、AI 提交和 NyxID 一次性授权接口。实现继续以 Python、MuJoCo、NumPy/Numba 和 Three.js 为主，4060 承担长回放与批量评估，Mac Studio 承担交互服务；只有 profiling 证明必要时才迁移 C++/CUDA。

**当前状态（2026-09-19，依据最近执行交接）**：Goal 仍在进行中。PR #51（封闭果园）与 #52（身体/脑活动同屏、固定活动色标、区分缺失记录）已合并；本地源码为 `73d5539`。PR #53 已确认合并，修复透明摄食探针错误碰撞墙壁和对手的问题。已有三维回放、同步感觉与脑活动、局部脑图、设计快照和演化展示入口；核心缺口仍是持续、可理解的生命行为和部署后直接可看的体验。旧版 30 秒双体回放在约 1.9 秒后双方越界并关闭运动驱动，不能作为持续竞争成功样本。封闭果园完整连接组实验 attempt 2 在上次交接时运行至 67%，本次整理没有重新查询进程，不将其记为已完成。视觉和触觉编码仍是工程模型；当前触觉输入只覆盖口器与食物接触，墙壁接触尚未输入神经网络。NyxID 最近返回会话过期，最新远端部署、4060 渲染与浏览器视觉验收尚未完成。

## 本轮执行清单与交付边界

沿用会话中已有的 active Goal。本文件是其可阅读、可继续执行的方案；历史实验记录保留在后文，冲突时以本节和上方状态为准。

- [x] 写明产品目标、最小架构、分阶段交付、算法边界和行为验收。
- [x] 合并封闭果园、身体与实际脑活动同屏的界面增量。
- [x] 确认摄食探针碰撞修复 PR #53 已合并。
- [ ] 保留修复前实验，完成同条件修复后对照。
- [ ] 完成并分析持续觅食样本：移动、接触、摄取、后续行为均有实际记录；失败样本如实展示。
- [ ] 发布可直接观看的三维生命回放，验收身体、感觉、脑图、事件的同一时间点联动。
- [ ] 完成设计后的固定刺激对照，让用户看到所改连接与响应差异。
- [ ] 跑通自己的果蝇与 WT/公开设计的真实资源竞争。
- [ ] 展示实际运行的不同优化算法、逐代候选、亲代对比；开放受资源限制的自定义策略沙箱。
- [ ] 贯通 AI 提交、生命谱系和首次使用指引；保留简单的 NyxID 身份接口，正式对接按用户后续安排进行。

**第一份用户可见成果的展示脚本**：打开回放 → 选中自己的果蝇 → 看到环境与身体 → 点击一次真实食物摄取事件 → 同步查看当时的感觉输入和脑活动 → 展开相关神经群及连接 → 查看自己的权重修改 → 打开 WT 或亲代的同条件对照。活动必须来自仿真记录，未采样处显示“未记录”。此流程实际可用后，再按上述清单扩展演化和竞技内容。

**实现主线**：`FlySpec → 感觉编码 → 连接组动力学 → 动作解码 → MuJoCo 身体与环境 → 同步记录 → Three.js 身体 + 脑图 + 时间轴`。优化器生成下一批 FlySpec 并复用同一个评估入口。记录实验条件和谱系即可支持研究复查，不额外引入逐文件审批或多轮人工评审。

**执行约束**：优先把长实验排到 4060；未实际连接节点时明确执行位置，不把本机实验称为远端运行。复用现有计算任务，先检查状态与产物再恢复，不重复启动昂贵实验。每个可用增量完成必要 CI 后及时合并、部署并给出入口；总 Goal 在真实用户流程验收前保持 active。

## 执行 Goal：让用户看见自己设计的大脑如何形成行为

**用户故事**：我设计一只果蝇的大脑，把它放进有食物、遮挡和对手的三维环境；我能看到它实际接收了什么感觉输入、哪些神经群活动改变、身体怎样行动、有没有吃到食物或赢得竞争；随后选择一种演化算法生成后代，并对比祖先、WT 和其他人的果蝇。

**下一次可见交付**：提供一个可以直接打开的回放页面，主画面显示果蝇身体与三维环境，旁边是可点击的大脑图，下方是共享时间轴。点击一次食物接触或摄取，身体、感觉值、脑区活动和资源变化跳到同一时刻。每只参赛果蝇都绑定自己的设计快照；缺失神经记录明确显示缺失，不能用装饰动画代替。

**关键行为验收**：30 秒只是观察窗口，不是成功标准。至少展示一次从出生区之后的移动到食物接触、摄取的完整过程，并按每 5 秒窗口呈现位移、摄取、能量和失败事件。若只在出生点吃到一次、随后静止、顶墙或越界，保留为失败或有限能力样本，继续改进感觉—运动闭环，不标为持续觅食完成。双体竞争需要双方确实影响共享资源或发生有记录的互动；演化改善需要在相同评估条件及未参与优化的种子上与 WT/亲代比较。

| 工作包 | 具体实现 | 用户验收 |
| --- | --- | --- |
| 持续生命行为 | 先处理封闭果园运行中断，再检查输入、神经输出、转向、接触和摄取；根据真实失败样本调整模型或任务 | 能看见探索、接近、接触、摄取及后续行为；卡住、翻倒、越界清楚可见 |
| 身体与脑同步 | 三维身体、感觉覆盖层、脑活动图和事件共用仿真时钟；支持暂停、慢放、拖动、事件跳转 | 点一次事件就能同时观察环境、身体和脑的响应 |
| 可编辑的大脑图 | 全脑区域 → 神经元类别 → 局部真实连接；分别显示权重修改和实际活动；固定刺激下对比 WT | 用户能找到自己改过的连接，并观察修改前后的响应差异 |
| 实际竞技 | 自己的设计、WT、公开设计共用同一地图和规则；先资源争夺，再扩展攻击/逃避 | 两个脑图和身体同步对比，结果可追溯到实际行为 |
| 可选演化策略 | 先提供真实运行的随机搜索、遗传策略、CEM 案例；自定义算法在沙箱生成候选，统一调用环境评估 | 能选择算法、查看逐代轨迹、打开后代回放并与亲代 PK |
| 生命账本与 AI | 记录父代、FlySpec 差异、算法配置、环境、随机种子、行为和结果；API 提供规则、提交、评估、回放读取 | 人与 AI 都能贡献候选，失败和成功样本都能沿谱系查到 |
| 指引与登录接口 | 首次进入解释连接组来源、神经模型、设计预算和实验步骤；NyxID 提供独立身份适配接口 | 新用户知道在设计什么、如何贡献、模型哪些部分来自数据；登录联调留待用户安排 |

**算法边界**：可修改权重、神经元参数和受支持的学习规则。神经动力学模型与候选优化算法分别选择，并标明兼容组合。随机搜索、CEM 等可调用非可微仿真；Adam/BPTT 只有在可微脑模型或明确声明的代理目标上才开放，不能宣称能直接对任意 MuJoCo 比赛端到端反传。最终表现仍由同一真实仿真评估。

**最小工程边界**：沿用现有代码，不为本轮重写 C++ 或新增微服务。网页与 AI 共用 FlySpec；训练策略共用评估入口；NyxID、Heca 和可选 Ornn 位于身份、算力及 agent 工具层。4060 优先排队执行长实验和批量评估，Mac Studio 提供交互服务；节点不可用时明确报告实际执行位置。只运行与改动相关的 CI 和必要的用户流程检查，每个可用增量及时合并、部署并交付可打开的入口。

**完成判定**：必须实际走通“设计 → 刺激预览 → 三维比赛与同步脑图 → 选择算法 → 查看后代 → 与 WT/亲代对比”这条用户路径，并展示至少一组有完整行为记录的演化样本。接口存在、测试通过、文件达到 30 秒或神经图发亮，都不能单独代替这项验收。观察到的活动相关性不自动证明因果；需要时通过同条件对照、连接扰动或消融来检验解释。

下面的方案与历史记录提供实现细节；当前状态以上方最新核对和文末交接为准。

## 执行摘要与下一次交付

**用户要看到的变化**：设计自己的大脑 → 放入三维场景 → 看到它感知食物、脑活动变化、转向和摄取 → 与 WT 或另一只设计对比 → 选择演化算法并观察后代。界面里的亮度必须来自实际神经记录，不能根据动画或得分补造。

| 顺序 | 用户可见的交付 | 验收条件 |
| --- | --- | --- |
| 1 | 一条 30 秒以上的觅食生命回放 | 有真实接触与摄取；可暂停、拖动时间轴；身体、感觉输入、脑活动和能量同步；提供可打开的页面 |
| 2 | 可展开的大脑图 | 从功能区域到神经元类别再到局部连接；区分设计修改、实测活动和未记录数据；点击事件定位同一时刻 |
| 3 | 设计后的刺激预览 | 修改权重后可运行相同刺激，对比 WT 的活动和运动输出；显示预算与修改位置 |
| 4 | 双果蝇资源竞争 | 自己的果蝇与 WT 或公开果蝇同场竞争；比较各自感觉、脑活动、摄取和结果 |
| 5 | 可选算法与生命谱系 | 用户选择内置或自定义策略；展示真实运行的算法案例、祖先与后代差异及比赛回放 |
| 6 | AI 贡献与简单登录接口 | AI 能读取规则、提交 FlySpec、运行沙箱并读取结果；NyxID 身份适配器独立于仿真，正式登录联调按用户后续安排进行 |

这是一条持续交付顺序，不要求等六项全部完成才发布。每个可用增量通过相关 CI 后及时合并并更新预览；不新增逐文件审批或重复评审。研究记录保留样本、父代、参数、算法、环境、输入、活动、行为和结果，重点是能复查生命演化过程。

**任务交接**：此前 4060 实验目录 `/tmp/fly-arena-multimodal-48-ceaa4f8` 的最终状态本次未重新查询，不能据此声称远端已有新回放。最近本机封闭果园实验的实际终止状态见文末；继续执行时先检查已有产物和任务状态，再决定恢复或重新排队，避免重复启动。

**最小架构**：网页设计和 AI 共用 FlySpec → 参数与预算检查 → 感觉编码、连接组动力学、动作解码和 MuJoCo 环境循环 → 统一回放记录 → 三维场景与脑图同步展示。演化策略反复调用同一个评估入口，生命账本记录每次候选与父代。沿用 Python/Numba、MuJoCo 和 Three.js，先不重写 C++；Mac Studio 提供交互服务，4060 优先排队执行长实验与批量评估。

**科学说明也是页面指引的一部分**：公开连接组提供真实结构约束，神经动力学、突触强度、感觉编码和身体控制仍包含模型假设。“基于真实果蝇连接组”不等于“编辑后的数字果蝇与真实果蝇完全一样”。页面应说明数据来源、采用的模型、已验证能力与实验性质。

## 用户可见的研究承诺

这个项目的交付单位是一份可以被观看和复查的数字生命记录。用户设计或演化出的每一只果蝇，都必须能在三维环境中面对明确情境，产生感觉输入，经过自己的连接组和神经参数形成脑内活动，驱动身体动作，并留下可解释的行为结果。回放中要能回答“它看见/闻到/碰到了什么、哪些脑区亮了、做了什么动作、为什么得到这个结果”，而不是只展示一条轨迹、一个最终分数或一个没有生命过程的排行榜名次。

网页设计、AI 提交和演化算法共享同一个 `FlySpec` 与同一套真实环境评估。用户可以修改连接组上的权重、神经元参数和可选学习规则，也可以选择遗传算法、CEM、随机搜索、模拟退火、Adam/BPTT 或自定义策略；每一次修改都必须沿谱系记录，能和野生型、其他用户以及不同代的后代进行真实比赛对比。竞技地图必须是有碰撞、资源、遮挡、能量和事件的三维场景，并逐步支持觅食、资源争夺、攻击/逃避和合作等任务。

这份 Goal 的验收顺序是“先让人看见并使用真实闭环，再扩展研究能力”：先完成带有食物接触和摄取事件的单体生命回放，再完成分层脑图与设计后预览，然后加入双体竞技、可选演化算法和 AI 贡献。NyxID 只提供一次性授权、身份、权限和额度接口，不进入科学仿真核心。任何尚未完成生理校准的视觉或触觉信号，都必须以明确 provenance 的工程化观测呈现，不能被 UI 或排行榜说成已经验证的生物学神经编码。

## 目标

把 Fly Arena 从“提交果蝇、跑一段轨迹、得到一个分数”推进成可观察的数字生命实验场。用户应该能够设计一张基于真实果蝇连接组的大脑，把它放进三维环境，观察感觉输入、神经活动、身体动作和任务结果在同一条时间线上发生，并沿着生命谱系记录每一次设计和实验。

第一阶段的硬验收物是：一只用户设计的果蝇在三维场景中完成一次可见、可复现、可审计的觅食过程。它需要检测食物的视觉或气味线索，移动到食物附近，完成接触和摄取；用户同时能看到果蝇、食物、传感器、行为事件、大脑活动和最终结果。

## 当前版本与目标的差距

当前版本已经有 MuJoCo 物理场景、Three.js 三维显示、连接组编译、神经 trace、生命档案和演化记录，但回放仍主要显示身体轨迹、少量回路曲线和最终分数。现有短时实验没有稳定的食物接触与摄取机制，也没有把“传感器输入 → 脑区活动 → 动作 → 结果”串成一条可读的生命记录。

因此下一阶段的重点是可见的生命闭环，而不是继续增加孤立的优化器、账号或发布接口。

## 产品体验

用户打开一只果蝇的生命回放后，应看到：

- 具有身体、触角、足和口器的简化三维果蝇；
- 叶片、果实、障碍物、光照和多个食物源组成的三维环境；
- 食物产生可记录的空间气味场，并有视觉、嗅觉、触觉和接触传感器；
- 第一人称、跟随视角、自由摄像机和脑图视角；
- 可暂停、慢放、逐帧和拖动的时间轴；
- “发现气味”“改变方向”“进入食物范围”“接触食物”“成功摄取”等事件标记；
- 同一时刻的身体状态、传感器读数、脑区活动、动作输出和能量/食物变化。

用户点击一个事件时，界面应解释这一小段过程：例如检测到左前方气味后，嗅觉回路活动上升，下行运动回路随后偏转，若干百毫秒后进入食物接触范围。解释必须区分仿真观测、行为统计和研究者解释。

## 大脑可视化

165,122 个神经元和 25,563,197 条连接不能一次性以毛线团形式绘制。采用可展开的三层视图，同时保留完整真实连接图：

1. **全脑功能区域**：视觉、嗅觉、触觉、中央复合体、蘑菇体、学习/记忆、下行运动和输出等区域。颜色和亮度来自该次仿真的活动。
2. **神经元类别**：按 class、side、region 和投射方向展开具体类别，例如嗅觉投射、Kenyon cells、descending neurons 和 motor-related neurons。
3. **局部神经元与连接**：显示实际节点、兴奋/抑制方向、突触权重、输入输出以及用户修改的边。

LIF 使用脉冲密度和膜电位，rate 模型使用连续活动值和相对基线变化。前端可以统一显示“活动强度”，但必须保留原始模型语义，不能把连续 rate 虚构成 spike。

## 生命回放数据契约

每个采样点需要记录：

```json
{
  "t": 12.34,
  "body": {"position": [0, 0, 0], "heading": 1.42, "velocity": [0, 0, 0], "energy": 0.82},
  "sensors": {"olfaction": [], "vision": [], "touch": [], "food_distance": 0.34},
  "brain": {
    "circuits": [{"id": "olfactory", "activity": 0.71}],
    "neurons": [{"id": 1823, "activity": 0.92}],
    "top_edges": [{"edge": 77312, "flow": 0.81}]
  },
  "events": [{"kind": "odor_detected", "source_id": "food-03", "confidence": 0.8}]
}
```

浏览器不需要每帧接收全部连接。身体和传感器可按 10–20ms 记录，回路活动按 10–50ms 记录，事件窗口记录 top-k 神经元和边；完整活动作为可下载研究数据保存。每份回放必须绑定 connectome hash、brain model、mutation spec、optimizer、map、seed、physics version、decoder version 和 simulation commit。活动亮度只能来自这些记录。

## 竞技任务

先做完整的觅食任务，再扩展竞争：

### 觅食

- 30–60 秒三维环境；
- 2–5 个食物源和不同气味强度；
- 部分食物被障碍物遮挡；
- 食物有限，果蝇有能量消耗；
- 接近不等于吃到，必须完成口器接触和停留条件；
- 摄取增加能量和得分；
- 越界、卡住和能量耗尽有明确失败事件。

### 竞争与躲避

在觅食机制稳定后加入两只或多只果蝇、资源争夺、接触/攻击、受伤、逃避和资源控制。每个地图必须明确观察输入、可执行动作、成功/失败条件、分数以及需要记录的行为事件。

## 设计和训练

设计页分为两个层级：

- 普通模式修改嗅觉、视觉、记忆、探索、攻击、逃避和运动等功能回路，并显示真实脑区、预算和影响说明；
- 高级模式按神经元类型、区域和左右侧搜索连接，查看突触数、权重和修改前后差异，导出完整 FlySpec。

保存前提供固定刺激预览：左侧食物、右侧食物、气味增强、障碍物接近和对手出现。预览和正式竞技必须使用相同的脑模型和解码器。

训练算法仍然可选：随机搜索、CEM、遗传算法、Adam/BPTT、模拟退火和用户自己的插件。所有候选都必须经过真实环境评估，并同时记录神经目标损失、真实任务得分、摄取数、路径长度、能量消耗、传感器使用和失败原因。神经响应损失下降不能自动标记为竞技性能改善。

## 页面结构

回放页面采用四个同步区域：

```text
┌──────────────────────────────────────────┐
│             三维竞技场                    │
│       果蝇、食物、气味、障碍物、对手        │
├───────────────────┬──────────────────────┤
│ 行为与传感器曲线   │ 大脑活动图            │
│ 事件与状态         │ 回路→神经元→局部连接  │
├───────────────────┴──────────────────────┤
│ 时间轴：感知、决策、行动、接触、摄取、结果  │
└──────────────────────────────────────────┘
```

必须支持事件跳转、场景对象筛选、脑区筛选、两只果蝇对比、视频导出和原始 JSON/Parquet 下载。

## 分阶段验收

### 阶段 1：生命回放最小闭环

一只果蝇、一个觅食地图、30 秒以上仿真、传感器、食物接触/摄取、行为事件、三维身体和同步脑区活动。验收标准：用户能打开链接看到果蝇找到并摄取食物，并在同一时间轴看到相关活动。

### 阶段 2：大脑图浏览器

完成全脑区域、神经元类别、局部连接三层展开，显示真实突触权重、设计修改和回放活动。验收标准：用户能从摄取事件跳到对应脑区和局部连接。

### 阶段 3：设计后即时预览

设计页显示脑图，修改权重后显示差异，在固定刺激下预览活动传播和运动输出。验收标准：用户能看到一次修改如何改变大脑和动作。

### 阶段 4：真实双体竞技

两只果蝇同场争夺资源，各自保留完整事件和脑活动，可按时间比较大脑与行为。验收标准：比赛结果能由感知、转向、接触、摄取和能量过程解释。

### 阶段 5：行为目标演化和 AI 贡献

优化器针对真实任务目标进化后代，页面展示每代脑图差异和实际回放；AI 通过 API 提交候选，记录优化理由、参数和结果。验收标准：能从祖先追踪到后代，并判断改变是否真正改善了任务。

## 技术路线

暂不重写 C++。继续使用 Python、MuJoCo、NumPy/Numba、Three.js 和现有 API；4060 用于长任务、批量评估和高质量回放渲染，Mac Studio 提供交互服务。只有 profiling 证明神经计算热点并且接口稳定后，才考虑把明确的热点迁移到 C++/CUDA。

## 第一项必须交付的可见成果

下一次交付不能只是接口、数据库或统计分数。必须交付一条可以观看的生命记录：一只用户设计的果蝇在三维森林场景中检测气味、找到食物、接触并摄取；用户可以拖动时间轴，同时看到果蝇、食物、传感器、事件和大脑活动，点击事件可以展开相关脑区和连接，并下载对应的可审计研究数据。

## 当前实现进度：回放神经剧场第一增量

已完成第一条数据与界面增量：仿真每个记录帧现在保存左右气味、基于 MuJoCo 三维射线与障碍遮挡计算的左右视觉目标观测、MuJoCo 物理接触、最近食物距离和口器距离。视觉明确标记为 `raycast_engineered_observation_v1`；食物触碰来自注册的口器接触探针与静态食物几何之间的 MuJoCo contact buffer，标记为 `mujoco_food_contact_observation_v1`。默认 `odor-only-v1` 神经桥只使用已校准的双侧气味行为基线，视觉和触碰只进入回放与事件记录；明确选择 `engineered-multimodal-v1` 的实验回放才把这些观测送入声明的视觉/触觉 canonical 群组。每帧同时保存各功能回路活动和固定的规范神经元采样，`events.json` 记录气味检测、视觉目标、物理食物接触、摄取、出界和个体接触事件，裁判会校验这些事件的结构和数值。

回放页已增加 Neural theatre：传感器状态、可选择的功能回路、从 `/connectome/neurons` 读取的规范神经元/局部边显示样本、基于真实活动的节点亮度，以及可以跳转时间轴的事件按钮。为了保持历史回放和现有浏览器契约兼容，首屏事件时间轴从同一批记录帧推导；不可变的 `events.json` 仍然作为研究证据和下载资产保留。当前活动图仍是“功能回路 + 局部连接样本”，完整连接图继续只在仿真端运行。

导出按钮现在生成 `match-observations/v2`：每个样本包含 `senses`、`brain`、回路活动、固定 canonical 神经元样本、身体状态和食物余额，顶层包含事件时间轴与 receipt 哈希。它明确说明固定样本不是完整 165,122 神经元 trace；完整原始文件仍以不可变 run asset 提供。

这一步让“感觉输入 → 神经活动 → 身体行为 → 结果”有了同一份记录，但还没有宣称完成第一阶段最终验收：需要在 30 秒以上场景中生成一条确实发生食物摄取的公开生命回放，并完成部署后的肉眼检查。下一步优先跑这条可观看的觅食样本，而不是继续扩展后台接口。

## 当前实现进度：版本化多模态神经输入桥

已加入 `engineered-multimodal-v1` 实验 profile。它把回放中的左右视觉射线值和 MuJoCo 口器接触值，分别写入 MaleCNS 的视觉左右群组和官方 `neurons.json` 中的 `mechanosensory_tactile` 群组；嗅觉仍然写入左右 ORN 群组。profile、输入通道、增益、来源 provenance 和 canonical 群组哈希会进入 runtime 与 receipt。没有触觉注释的旧图谱会 fail closed，不能把 touch 电流广播到任意神经元。

独立的 `EmbodiedSensor` 在每场实验开始时构建群组映射，并接入 LIF 和 rate 模型。原有 `Brain`、`RateBrain`、`CPUBrainBackend` 保持主分支实现，研究 v2 的校准身份不受影响。默认 `odor-only-v1` 保留原有行为；实验 profile 才启用视觉/触碰电流。目前多模态仅支持 legacy bridge，网页可以选择并提交实验运行，结果可回放但不进入排行榜。未知左右侧不按数组顺序猜测；触觉仅采用明确的 `mechanosensory_tactile` 标注。

真实 MaleCNS + MuJoCo 的 1 秒实验回放已经通过独立裁判：

```text
profile:       engineered-multimodal-v1
receipt:       30f3f53774a03848533295ec47d1b010e99ffbd58dda32a79dce681ad3015850
final_tick:    10000
judge:         verified / event-conservation-v1
visual_frames: 100
touch_frames:  0 (该种子和出生位置没有发生口器 contact)
```

该次运行包含 165,122 个神经元、25,563,197 条连接，场景保存实际群组映射摘要，每帧保存送入大脑的输入值。完整后端回归为 `451 passed, 4 skipped`，包括研究 v2 校准身份检查；前端 51 项测试、10 项用户流程测试及构建通过。

这证明多模态输入已经进入真实完整连接组的神经状态，并被 receipt 绑定；它不证明视觉或触觉编码已经具有生物学有效性，也不代替下一步必须交付的 30 秒以上、确实摄取食物的公开生命回放。

## 统一研究对象：可演化的数字果蝇

Fly Arena 的基本研究对象不是一个抽象控制器，而是一只带有真实连接组来源、可修改神经参数、身体、感觉和生命记录的数字果蝇。公开的果蝇连接组提供 canonical brain：神经元、突触数量、兴奋/抑制方向和脑区归属保持可追溯；用户或 AI 提交的是在此基础上的 `FlySpec`，不能覆盖原始连接组。

连接权重从 canonical 值开始：

```text
W0(i,j) = synapse_count(i,j) × sign(i,j) × base_strength
W(i,j)  = W0(i,j) × mutation(i,j)
```

普通用户通过嗅觉、视觉、记忆、探索、攻击、逃避和运动等功能回路编辑器修改回路倍率；高级用户按神经元类别、区域、左右侧或边组编辑；AI 和研究者可以提交完整的权重差分、神经元内在参数和可选的学习规则。三种入口最终都编译成同一种 `FlySpec`，因此网页设计、API 提交和自动演化可以直接比较。

每份 `FlySpec` 都有 mutation budget。预算按照连接重要性和 `abs(log(scale))` 计费，强化或削弱不能无限叠加；预算、修改前后权重摘要和约束版本会进入不可变记录。这样优化目标必须在“改变哪里”和“保留什么能力”之间做出策略选择，而不是把所有兴奋权重调到最大。

## 人、AI 与演化算法的同一条路径

用户可以在网页里设计一只果蝇，也可以选择算法在沙箱中生成后代。第一批内置算法包括随机搜索、遗传算法、交叉熵方法（CEM）、模拟退火、Adam/BPTT（仅对可微代理或 rate 模型开放）和一个自定义算法接口。算法不是隐藏的后台实现，而是实验配置的一部分：名称、版本、随机种子、父代、mutation budget、候选数量、评估地图和停止原因都要随实验写入生命账本。

每个候选必须在真实竞技环境中评估，同时记录任务得分、摄取数、路径长度、能量消耗、传感器使用、失败事件、神经目标损失和行为目标损失。神经响应更接近目标不能自动等于竞技表现改善；排行榜只接受通过相同地图、物理、脑模型、解码器和随机种子协议产生的可复现实验。

演化记录采用谱系图而不是只有“第几代”的计数：每个样本有稳定 ID、父样本 ID、算法、变异摘要、完整 `FlySpec` 哈希、回放资产哈希和裁判结果。初代是明确的 `generation: 0`，每个后代的代数等于父代最大值加一；没有父代的候选不能被显示为第三代或被伪造为已进化样本。用户可以沿谱系查看每一代的脑图差异和真实比赛回放。

## Arena 是研究沙箱，也是可解释的比赛

地图必须是三维、可渲染且有明确规则的实验环境，而不是一维轨迹播放器。觅食地图先提供叶片、果实、障碍物、多个气味源、能量消耗和遮挡；随后加入资源争夺、双体接触/攻击、逃避和合作任务。每个地图版本定义传感器、动作、碰撞、摄取、越界、能量和计分规则，并把规则版本绑定到回放。

每一场比赛都形成一个可审计的生命样本：环境状态、果蝇身体、感觉输入、脑区和采样神经元活动、动作解码、行为事件、分数和失败原因在同一时间轴对齐。事件必须能跳转到三维场景和脑图，例如“左侧气味上升 → 嗅觉回路活动上升 → 转向 → 口器接触 → 摄取”。没有经过生理模型验证的几何视觉或物理接触信号必须作为带 provenance 的观测呈现，不能被 UI 误称为真实视觉/触觉神经输入；实验 profile 的电流注入也必须保持独立版本、明确群组和非排行榜状态，不能悄悄改变已校准的神经行为基线。

## AI 贡献与 NyxID 接口边界

AI 贡献使用和网页设计完全相同的 `FlySpec`、评估和回放接口。AI 可以读取规则、连接组摘要、公开谱系和历史比赛，提交候选、启动沙箱实验、读取事件/脑活动并上传分析；它不能修改 canonical connectome、裁判规则或历史账本。AI 的提示、设计理由、使用的算法和结果摘要作为实验元数据保存，便于回答“AI 到底贡献了什么”。

NyxID 只负责一次性授权后的身份、权限和额度，不进入连接组仿真核心。预留的接口边界为：

```text
POST /api/v1/auth/exchange       # 一次性授权码换 Arena session
GET  /api/v1/me                  # 用户或 AI agent 身份
POST /api/v1/flies                # 提交 FlySpec
POST /api/v1/experiments          # 启动演化/评估沙箱
GET  /api/v1/experiments/:id      # 状态、日志和资源用量
GET  /api/v1/replays/:id          # 回放、事件、脑活动和 receipt
```

MVP 可以用本地 demo session；NyxID 接入时只替换身份适配器。权限至少区分 `fly:submit`、`arena:run`、`replay:read`、`lineage:read` 和管理员能力，GPU 时间、并发数和私有样本按 agent 记账。前端登陆应是一次性授权链接或短码，避免用户先理解复杂的账号系统。

## 生命账本与 TruTuring 的可借鉴部分

TruTuring 的可借鉴点是把运行过程当作可验证的 harness：输入、工具、版本、输出和判定必须形成结构化证据，治理规则应能在运行前检查、运行中记录、运行后复现。Fly Arena 将其映射为生命账本：`FlySpec`、connectome hash、map、seed、physics、brain model、decoder、算法、资源节点、事件文件、回放和 judge receipt 组成一条不可变实验链。

账本记录事实和来源，不替研究者解释因果。裁判只能验证结构、边界、规则和哈希一致性；神经科学解释必须标记为观测、相关性或研究者假设。版本化 CI 要求前端测试、后端测试、receipt 校验和最小真实回放通过后才能合并；每个增量尽快合并到主分支并发布可使用的版本，避免把工程流程变成与科研无关的逐文件审批。

## 资源与工程路线

Mac Studio 运行交互 API、网页和轻量预览；4060 节点优先运行全图神经计算、30–60 秒生命回放、批量候选评估和渲染。GPU 任务通过队列和单节点锁串行调度，前端能看到排队、运行、失败和产物链接。每次完成一条可观看的回放就发布一个可用增量，不等待所有竞技模式完成。

先保持 Python、MuJoCo、NumPy/Numba 和 Three.js 的接口稳定。只有 profiling 明确指出神经计算热点，且 Python 版本已有可复现实验和测试，才把独立热点迁移到 C++/CUDA；C++ 不是第一阶段的交付条件。

## 当前执行顺序与“可见成果”门槛

1. 修正观测与神经输入的边界，保持已校准的 odor-only 行为基线；固定脑图显示样本，使活动按 canonical neuron ID 映射，未采样节点显示为未采样。
2. 已加入 MuJoCo 口器接触探针和食物 contact buffer 读取；下一步要把同一证据展示在公开回放中，并完成浏览器肉眼检查。
3. 已生成并登记一条 30 秒、确实发生接触和摄取的正式多模态生命回放；当前 4060 的 NyxID DNS 暂不可用，因此这条证据由 Mac Studio CPU 完成，仍绑定完整运行时和独立裁判。下一步需要完成浏览器肉眼检查与更复杂地图的同等验收。
4. 合并设计页的 FlySpec 与 mutation budget，提供固定刺激预览和真实环境评估。
5. 接入随机搜索、遗传算法和 CEM 的可选实验配置，展示至少两条真实演化谱系。
6. 增加双体竞技、脑图对比、AI 提交和 NyxID 身份适配器。

每一步的完成条件都是用户可以打开页面观看、拖动时间轴、看到三维行为与脑活动对应，并下载 receipt；只有新增接口、统计分数或漂亮但没有真实数据的回放不算完成。

## 当前实现进度：设计后神经预览

在分层 Neural theatre 之后，设计页现在有“预览神经反应”入口。它把当前未保存的 `FlySpec` 编译成临时不可变权重工件，在完整 MaleCNS 图上运行四组固定双侧刺激（左、右、双侧、空输入），返回每组功能回路活动和 LIF 脉冲总数；连续 rate 模型保留 `total_spikes: null` 的模型语义。预览明确没有身体、分数、排名或比赛结论，页面同时显示模型、工件摘要和连接组版本。

本地真实全图验证（2026-09-19）返回 `neural-design-preview/v1`，165,122 神经元、25,563,197 条边的 LIF 预览中，左/右/双侧刺激分别记录了非零脉冲，空输入记录 0 脉冲；这证明设计后的参数已经进入实际连接组动力学。该预览仍是固定气味电流刺激，不能替代三维身体回放，也不能把工程模型解释为生物学视觉或触觉编码。

## 阶段一长回放证据

第一条达到阶段一时长要求的长回放已经在 4060 节点完成，并通过本机独立裁判：

```text
match:       7eaac6f54a0c4e9a9a0067c5dfd0b4ae
map/mode:    orchard / forage
fly:         e4c854ea6f16475786fb7b5f66bce426
duration:    30.0 s
frames:      3001 (tick 0 → 300000)
status:      verified
score:       1.3984
receipt:     e5d230721163763d2ec8f1314cc24a712938c219ca2f8c80659f91bf0b677cdb
```

事件时间轴包含气味检测、几何视觉目标观测、口器接触、5 次摄取和出界；完整 `brain-0.npz`、`physics.npz`、`frames.json`、`events.json` 和 `receipt.json` 均保存在本机预览的不可变 run 目录。回放有 3001 帧、48 个固定 canonical 神经元样本，采样活动中有 98,756 个非零值，最大值为 444.4147。receipt 继续明确声明：嗅觉是当前神经输入，视觉是工程化观测，旧回放的触碰也是距离观测；这条记录证明了长时可审计的生命闭环和数据链，还不证明生物学视觉或触觉已经完成校准。

射线视觉增量也在 4060 的隔离 staging `/tmp/fly-arena-raycast-v1` 中完成了短回放验证。该回放为 1 秒、101 帧、`final_tick=10000`，状态为 `verified`，receipt 为 `f0bad79cc2bb0de4f44eb3dd146992009f12829fe9b5375ff756c61d2aa76d15`。receipt 的视觉传感器版本为 `raycast_engineered_observation_v1`，场景 body manifest 中包含 5 个 food geoms；整段有 178 个非零视觉采样，最大值为 1.0，视觉事件标记为 `raycast_engineered_geometry`。这证明射线观测已经在完整连接组和 MuJoCo 场景中运行并被裁判接受，仍然不把工程化视觉观测冒充为已经校准的视觉神经输入。

物理触碰的隔离仿真证据在本地 MaleCNS artifact 上完成。目录 `/tmp/fly-arena-mujoco-contact-replay-near` 使用 1 秒、10000 physics ticks 的单体 forage run；receipt 为 `c9248ce565196ac9df7685d1c068d7d763acb03ced80c5bc130ccfed177a2feb`。在食物附近出生的受控场景于 tick 0 记录 `food_contact`，事件包含 `observation: mujoco_food_contact` 和 `food: ["food-0"]`；101 个 replay frames 中有 2 帧触碰为 ON，所有帧的 provenance 为 `mujoco_food_contact_observation_v1`。这条记录证明触碰事件确实来自物理 contact buffer；该受控场景使用静音隔离运行，尚未作为标准地图的排名证据提交。它仍然是工程化的接触探针观测，不等于已经完成触觉神经编码或生物学验证。

本次接触增量的验证结果：后端完整测试 `441 passed, 4 skipped`；前端 `npm run build --prefix web` 成功。新增的物理接触单元测试同时检查食物 contact geom、口器接触探针、遮挡射线和有限物理 rollout。构建仍有 Vite 关于 bundle 大小的提示，但没有类型或生产构建错误。

## 当前公开回放：标准场景生命闭环短验证

在修正口器 contact probe 后，Mac Studio 本地生成并通过独立裁判了一条标准 `orchard / contest / seed 42` 回放：

```text
match:       de796b637f094429b763c3bfc9260211
duration:    2.0 s
flies:       Wild Type / Nectar
status:      verified
receipt:     131dc85f9988cb478cf53d939d365c0dd6b85b81dbb486909417c835b546651b
scores:      [0.3120, 0.0608]
```

这条回放已经把同一时间轴上的事件闭合起来：双方在 tick 0 产生嗅觉和射线视觉观测，tick 6400 由 MuJoCo contact buffer 记录 `food_contact`，tick 6500 发生摄取；随后保存食物余额、能量、身体位姿、两只果蝇的物理接触、脑区 trace 和 receipt。回放目录位于 `var/runs/de796b637f094429b763c3bfc9260211/1`，API 可通过 `/api/v1/matches/de796b637f094429b763c3bfc9260211` 和对应的 `scene`、`frames`、`events`、`receipt` 端点读取。

这次修正解决了一个会让果蝇停滞的物理错误：透明口器探针此前同时碰撞地面并带有默认质量，改变了身体惯量。现在探针是零质量、只与食物几何接触，并且半径 `0.65 mm` 与 `0.45 mm` 食物球之和匹配 `1.1 mm` 的摄取阈值。`tests/test_terrarium.py` 与 `tests/test_embodied_sensor.py` 的相关检查已通过。

这条记录是标准地图上的真实接触→摄取短验证，已经可以作为网页回放样本；它仍然不能替代第一阶段要求的 30 秒以上公开长回放。当前 `odor-only-v1` 将视觉和触碰作为带 provenance 的物理观测保存，只有明确选择 `engineered-multimodal-v1` 才把它们注入声明的实验性神经群组。后续必须在不改变这些科学边界的前提下，把同样事件链延长到 30 秒并完成浏览器肉眼验收。

## 正式多模态短回放：视觉/触碰进入真实连接组

在确认口器 probe 的物理修正后，使用正式版本化的 `engineered-multimodal-v1` profile 重新运行了同一标准地图：

```text
match:              47fcd57d58154e6ba44e720683cf5423
map/mode/seed:      orchard / contest / 42
duration:           2.0 s
status:             verified
receipt:            1421fadbe161e852f9763403bd335e6a14802a0ed6db918dd1de64f2302ab676
visual_gain_mv:     0.1
touch_gain_mv:      0.1
food_contact:       tick 5700, slot 1, food-0
first_intake:       tick 6000, slot 1
food_collected:     1.4328
neural_input_frames: 400
touch_frames:       17
```

receipt 中的 profile 明确绑定了五个通道、MaleCNS 视觉左右群组、`mechanosensory_tactile` 群组哈希和实际增益。400 个记录帧都保存了送入脑状态的输入身份；224 个视觉观测帧和 17 个物理触碰帧被裁判保留。事件时间轴显示视觉/嗅觉检测、MuJoCo `food_contact`、摄取、果蝇间接触和出界，最终结果为 slot 1 胜出。

这证明工程化视觉和触碰观测已经可以进入完整 MaleCNS 神经状态，并在声明为实验性的低增益桥下闭合到真实接触和摄取。`0.1 mV` 是为了避免未经校准的直接电流压过现有嗅觉到运动读出而选择的工程参数，不是果蝇生理学标定值；profile 仍然标记 `ranking_eligible: false`。这条 2 秒记录仍不能替代 30 秒以上长回放和浏览器肉眼验收，但它已经是当前最完整的多模态生命闭环样本。

## 当前实现进度：脑活动与真实神经元显示样本对齐

回放固定采样现在使用每个 canonical 功能回路的前 6 个神经元，与 `/connectome/neurons` 的确定性展示窗口一致。这样 `frames.json` 中记录的 `sampled_nodes` 可以直接和网页请求的真实神经元元数据、局部连接及活动亮度连接起来；仿真仍运行完整 165,122 个神经元和全部连接，展示抽样不代表完整脑活动。该修复已合并到 `main`（`a77f47f`），后续新回放会使用这一对齐规则。

## 正式 30 秒多模态生命回放

在 Mac Studio 上用当前 `engineered-multimodal-v1` profile 完成了第一条连续 30 秒单体觅食记录。它没有把视觉、嗅觉、触碰或神经状态拼接到旧回放，而是从 tick 0 连续运行到 tick 300000，并通过本机独立裁判：

```text
match:               0aefa99328034f458d89aa68242364fe
fly:                 Nectar / 花蜜
map/mode/seed:       scarcity / forage / 42
duration:            30.0 s
status:              verified
receipt:             0d19f2b939c83159451c042161166af9f60282d08e656762d0d747814e7e0048
food_contact:        tick 3600, mouth_distance 1.0497 mm
intake:              ticks 4000, 4500, 5000; total 1.1232
visual_frames:       44
touch_frames:        13
neural_input_frames: 3000
judge:               event-conservation-v1
```

该记录含 3001 个身体/感觉/脑活动采样帧、完整的 `brain-0.npz`、`physics.npz`、`scene.json`、`frames.json`、`events.json` 和 receipt，并已经写入本地 match 索引，可以通过 `/api/v1/matches/0aefa99328034f458d89aa68242364fe` 及其 `scene`、`frames`、`events`、`receipt` 端点打开。它证明了当前工程模型下“感觉输入 → 完整连接组状态 → 身体移动 → 物理接触 → 摄取 → 能量/分数”在 30 秒连续回放里闭合；视觉和触碰仍然是带 provenance 的工程观测，不能解释为已完成生理校准。

回放请求现在同时读取不可变的 `events.json`，事件时间轴不再只从帧阈值推导。事件按钮会跳到事件之后的第一个身体采样点，因此点击 `food_contact` 会同时看到真实的 `Touch ON` 和相邻的摄取过程。使用这个正式回放的离线浏览器 smoke 已验证：生产构建加载 1 个三维 Canvas 和 30 秒时间轴，无 JavaScript/网络错误；点击 contact 后显示 `0.37s`、`Touch ON`、`Food contact` 和 `Food intake`。

同一批次还保存了一条 WT 在 `orchard / forage / 42` 中没有摄取的失败记录（match `f049421c0abf4c0a8f222ba3ed132d26`）。它同样是 30 秒、3001 帧并通过 receipt 校验，但结果为 0 contact、0 intake、一次出界。失败样本保留在生命档案中，用来显示设计差异和真实实验选择，不能被隐藏或改写成成功。

## 神经剧场增量：事件响应窗口

回放的生命事件现在不仅改变时间轴。用户点击气味检测、视觉目标、物理接触、摄取、出界或个体接触后，神经剧场会保存该事件，并显示事件时间点前后的实际记录采样：每个功能回路的活动值与差值、左右气味、左右视觉、触碰状态以及两个采样时刻。事件发生在 physics block 开头，响应窗口选择事件之后的第一个 pose sample，避免把同一个 block 当成前后变化。

该窗口只描述时间上的观测变化，不自动宣称因果。视觉仍标记为 `raycast_engineered_observation_v1`，触碰仍标记为 `mujoco_food_contact_observation_v1`；活动亮度和响应数值全部来自回放中的 `traces`、`brain` 与 `senses`，没有用动画或得分补造。切换参赛个体或回放时会清空旧事件选择，避免把一只果蝇的响应显示到另一只身上。

前端验证：`npm test --prefix web` 通过 51 项，`npm run build --prefix web` 通过。Playwright UI 测试在当前 Mac 沙箱启动 Chromium 时被系统 Mach port 权限拒绝，因此没有把浏览器验收冒充为通过；已有离线 mock origin 验收继续作为此前版本的证据。

## 演化展示增量：公开 gallery 不依赖源数据库

训练页的演化画廊现在支持读取部署时随应用提供的只读公开 gallery 文件。此前 `training-showcase` 只查询 SQLite 的 `training_publications`；当发布记录与应用数据库分开打包时，访客会错误地看到“没有示例”，尽管已经存在真实的 evolution、random search 和 CEM 运行。

服务端现在在没有对应 SQLite 发布记录时读取 `var/research/evolution-gallery-v038/*-public.json`，并通过同一公开数据返回三条已完成轨迹。画廊中的比赛 ID 也能解析到对应的只读 `scene`、`frames`、`events` 和 `receipt` 文件，所以用户从某一代候选点击回放时仍然进入真实比赛证据。该路径只读已发布文件，不将其当作私有训练会话，也不允许通过它写入数据库。

本地真实检查返回三个算法示例：`cross_entropy`、`evolution`、`random_search`；其中一个静态回放的 `matches/{id}/events` 返回真实事件记录。新增后端测试覆盖没有源数据库时的公开 gallery 与回放资产读取，并保持账户字段和 worker 字段不公开。

Arena 首屏的初始焦点现在按生命证据选择：优先选择至少 30 秒、使用 `engineered-multimodal-v1`、并且有实际摄取记录的已验证回放；若部署中没有这类样本，则退回到有正摄取结果的已验证回放，再退回任意已验证记录。最近比赛列表仍保留原始时间顺序，精选逻辑只影响首次进入时显示的样本，避免访客第一眼看到空结果或失败样本。

## 事件账本与结果摘要的一致性

回放结果中的 `contact_ticks` 继续表示两只果蝇之间的 MuJoCo 接触，不能把它解释成食物接触。新生成的回放同时写入 `food_contact_ticks` 和 `intake_ticks`，两者分别列出每个个体在不可变 `events.json` 中出现的物理食物接触与摄取事件 tick。独立裁判会在这些字段存在时逐项和事件账本比对；旧回放没有这两个字段时仍按旧 receipt 规则验证。这样单体觅食的“碰到食物”和“吃到食物”不会再被一个含义不明确的 `contact_ticks: 0` 掩盖，API、首屏精选和生命档案可以使用同一份事件证据。

## 双体神经剧场对比

竞技回放的观测面板保留单体聚焦模式，并增加显式的“Compare participants / 对比参赛果蝇”开关。开启后，同一条回放时间轴下并排显示每个 Slot 的摄取曲线、能量、运动驱动、身体高度、功能回路曲线和三层神经剧场；每一列仍使用该 Slot 的实际 `frames.json`、`brain`、`traces`、感觉输入和事件记录。关闭开关不会改变原有单体观察行为。该对比只对齐观测时间，不把胜负或神经活动解释成因果，也不会为缺失的帧或神经值填充数据。

训练页的每代候选现在可以从“Compete”直接进入带有训练地图、种子和时长的竞技设置。候选保存后，系统优先选择训练中配置的固定对手；如果训练是单体觅食，则自动寻找同一连接组和神经模型的 WT 作为对手，并进入 contest 模式。这样候选的演化评估条件和后续可观看的双体生命回放之间有明确入口；实际结果仍必须重新通过竞技场裁判，不能把训练分数当作比赛证据。

候选卡片还提供“与亲代对比”。如果谱系中的父代已经随训练记录返回，父代会被加入当前竞技选择列表，并作为第二个真实参赛者提交；没有可访问父代时按钮明确退回“与 WT 对比”。该入口保留候选的父代关系和 FlySpec，不把父代活动状态复制成后代状态。

回放页的神经与行为记录面板现在还展开 `Replay provenance`。它显示比赛的地图、模式、种子、时长、感觉 profile、参赛者 FlySpec 的 connectome/model 摘要、MuJoCo/机器运行时、读出权重摘要和 receipt 哈希；这些值来自比赛请求、已保存设计和不可变 `receipt.json`。receipt 端点不可用或历史字段缺失时显示 `Unavailable`，不会根据当前机器或页面状态猜测实验条件。

provenance 还会逐个 Slot 显示实际设计修改：功能回路倍率、edge delta 数量、τ 和 threshold 修改。缺失字段按历史 FlySpec 兼容规则显示 canonical/默认值，不会把当前编辑草稿当成已经参赛的设计。

## 正式 30 秒双体竞争回放

新增并登记了一条完整双体样本，作为 Arena 首屏精选回放：

```text
match:               aca9ac97af8a4cfcb863c3b45da5e857
attempt:             2
flies:               Wild Type / 原型 vs Nectar / 花蜜
map/mode/seed:       orchard / contest / 42
duration:            30.0 s
status:              verified
winner:              slot 1 · Nectar / 花蜜
scores:              [0.0, 1.4328]
food_contact:        slot 1, tick 5700
intake:              slot 1, ticks 6000, 6500, 7000, 7500
fly_contact:         ticks 6943, 7655
exit:                slot 1 at 18529; slot 0 at 18907
sensory_profile:     engineered-multimodal-v1
receipt:              d633961bc9c71798c6703c3609477a87aaa428a8d8ccc37b3dbb09794087002c
judge:                event-conservation-v1
```

这条记录从 tick 0 连续运行到 tick 300000，保存两只果蝇的完整脑 checkpoint、身体物理、感觉输入、回路 trace、规范神经元采样、事件账本和 receipt。独立裁判与 API smoke 已验证 `/matches/{id}`、`scene`、`events` 和 `receipt` 均返回 200；前端的首屏精选规则会优先选择这条 30 秒、多模态且有实际摄取的 verified 回放。slot 0 没有摄取不是失败数据，仍保留为 WT 的真实竞争结果。

## 部署可见性增量：精选回放只读资产

研究工作区的 SQLite、完整脑 checkpoint 和账户记录属于私有运行数据，不能随着源代码复制到 Mac Studio。为保证用户仍能打开真实生命样本，新增 `scripts/bundle_replay.py`：它从一条已验证比赛的单次 attempt 读取 `scene.json`、`frames.json`、`events.json` 和 `receipt.json`，先逐项验证 receipt 中的 SHA-256，再生成 `var/research/replay-gallery-v1/*-match.json` 及四个浏览器资产。包中明确记录比赛请求、结果、attempt 和 receipt 哈希，并排除数据库、全量 `brain-*.npz`、`physics.npz`、编译工件和账户字段。

API 的 `/matches`、`/matches/{id}` 和 replay artifact 路由现在同时读取这些只读精选资产。这样部署实例即使没有源 SQLite，也会把实际验证过的比赛列在首页并提供相同的三维场景、时间轴、事件和 provenance；公开资产不会成为可写训练会话。同步通过 `scripts/sync_mac.py var/research/replay-gallery-v1` 在维护窗口中显式执行，不会把 `var/` 默认打进源代码包。

当前本地已为 `aca9ac97af8a4cfcb863c3b45da5e857` 的 attempt 2 生成 68,547,647 字节精选包；它保持 30 秒双体竞争、slot 1 摄取和独立 receipt，不把旧回放与新回放拼接。NyxID 节点查询在 2026-09-19 仍因 `nyx-api.chrono-ai.fun` DNS 不可达而未能执行远端同步，因此没有声称 Mac Studio 或 4060 已更新；待连接恢复后按上述显式资产包流程同步并重新做 API smoke。

## 精选回放保留参赛设计

只读回放现在携带双方 FlySpec、工件 ID、预算和父代 ID。生成包前核对参赛者顺序、工件身份、请求与原比赛 receipt；前端神经剧场、修改摘要、谱系提示和观测导出优先使用这份记录，即使部署端果蝇库为空也可解释设计。它不会把这些样本写入账号或可训练果蝇库。

使用真实双体样本完成了隔离 API 验证：临时 Store 的比赛数为 0，列表、详情、scene、59 MB frames、events、receipt 均返回 200。该验证使用进程内 ASGI 请求，不代表远端部署或浏览器视觉验收完成。新增回归覆盖空果蝇库、同 ID 不同参数的库记录、缺失资产声明及参赛工件不匹配；31 项相关后端测试、52 项前端测试和生产构建通过。

## 多模态比赛接入计算节点

已移除 API 对多模态比赛的本机旁路，并让节点 `describe` 接收所选 sensory profile。新节点会返回完整的感觉编码配置；旧节点忽略新参数时会被配置对比拒绝。连接失败返回 503，不降级到仅嗅觉或本机运行。配置了节点的 Legacy 比赛按配置调度，即使节点与应用主机的运行时相同也交给节点；科研 v2 继续使用原来的本机执行路径。

本机完成了一次实际节点协议验证（不经过 SSH，不代表 4060 执行）：独立节点进程重新编译 Nectar，在完整 165,122 神经元、25,563,197 条连接上运行 `scarcity / forage / seed 42 / 1s / engineered-multimodal-v1`，传回回放后由独立裁判验证。101 个身体帧包含 100 个神经输入帧，接触 tick 3600，摄取 ticks 4000、4500、5000，总摄取 1.1232；receipt 为 `851df9603ace59bb9dd61c9f76aadf1a69ebb20c6c87f6edd5b9c837ab25b579`。摘要保存在 `docs/evidence/multimodal-node-smoke.json`，原始文件在本地 `var/research/node-multimodal-smoke/092c0b3a0f6b4d35b6a1bdb7e16176af/evidence`。

节点相关 14 项测试通过，覆盖多模态比赛/系列赛提交、配置不匹配、断线不自动改为本机执行、幂等重试和相同运行时的节点调度。NyxID 查询本轮仍无法连接服务器刷新会话，因此远端排队与实际部署验收仍未完成。

## 持续活动场景：封闭果园

已确认旧版 30 秒双体样本在约 1.9 秒越界后关闭了双方运动输出；后续约 28 秒记录不代表持续竞争。新增独立地图 `enclosure`（封闭果园），由 16 段相交的实体围挡包围五处共享食物，内部加入落叶和果壳。身体碰撞、可视场景和视觉遮挡共用同一份障碍物几何，不做位置回绕、自动转向或神经重置。旧地图、旧回放和原计分规则保持原意；如果果蝇翻越围挡仍会按越界规则记录。

网页地图预览、比赛/系列赛、训练条件和 AI 命令行均可选新地图。页面指引提示用户观察再次觅食、沿边运动、碰壁停滞与翻倒，明确“没有越界”不等于学会觅食。

物理原型在固定驱动下运行 5 秒，两只身体都留在场内，后 2.5 秒路径长度分别为 16.39 和 2.46 mm；这是围挡预跑，不是神经行为证据。地图/碰撞相关 28 项测试、53 项前端测试和构建通过。使用完整 MaleCNS 和实验性多模态输入的 30 秒 WT/Nectar 比赛已在本机隔离研究目录启动；结果须按每 5 秒实际位移、摄取、越界与驱动记录判定，不能在结果出来之前称为持续觅食成功。

## 最新交接：Goal 文档整理时核对（2026-09-19）

- 当前会话已有 active Goal，继续沿用，不重复创建，也不标为完成。
- PR #51 合并命令返回 `401 Unauthorized / Requires authentication`；CI 通过与合并完成是不同状态，后续需重新确认远端 PR 状态。
- 封闭果园实验 `177b18a418104697b2d9544168d0c873`（attempt 1）已异常退出：heartbeat 报 `Worker lease is no longer active`，随后 finish 报 `Rejected stale worker result`。不能继续把它描述为正在运行或已经成功。
- 隔离实验目录：`var/research/enclosure-pilot/9f3c60a2ee46407fb721a0148eea7830`。保留已有产物；下一步先查租约失效原因及可恢复状态，再排队完成真实行为样本。本次整理只更新文档，没有重启实验。
- 下一项产品交付仍是持续行为的同步回放与可展开脑图；远端部署、4060 新地图渲染和最新页面的视觉验收尚未在本次核实，不能记为完成。

## 同屏生命剧场：全脑功能活动概览

回放页在桌面宽度下把三维场景和神经面板并排显示，共享原有时间轴；新比赛设置折叠到下方，窄屏仍按场景、脑图顺序展示。脑图置于详细曲线之前，实验版本信息保留在可展开区。功能概览使用实际记录的群体平均活动，显示嗅觉、投射、局部、记忆、读出、视觉、下行和运动群组。轮廓与位置是功能示意，不是解剖坐标，也不绘制未经数据支持的区域连线。

点击功能群进入神经元类别，再进入原有真实局部连接图。群体与单神经元各自使用整段回放、双方共享的固定色标，避免每帧自动缩放制造活动变化。设计快照中的回路倍率用金色边框与倍率标记；局部图未采样节点为空心并显示“未记录”，不再把缺失活动显示成零。

55 项前端测试和生产构建通过，包括零活动与缺失记录的区分、同一色标随时间更新、双果蝇图形标识不冲突和原有参赛者身份。浏览器视觉验收尚未完成，不将 React 服务端渲染测试称为真实浏览器验收。

运行交接：`177b18a418104697b2d9544168d0c873` 的 attempt 2 使用本机 arm64 CPU；NyxID 再次连接失败，本机 Heca daemon 未运行，因此没有声称已在 4060 执行或已同步 Mac Studio。PR #51 已确认于 `2026-09-19T06:29:05Z` 合并。

## 物理修正：透明口器探针不再参与墙体与对手推挤

真实 MuJoCo 回归复现了一个碰撞过滤错误：口器探针虽然设置 `conaffinity=8`（只接受食物），但此前仍声明普通果蝇的 `contype`。MuJoCo 的双向匹配规则使墙和另一只果蝇仍能接受这个透明探针，因此它会额外参与碰撞。新增的围墙与双体重叠几何测试在旧代码上都失败；问题不是动画误差。

修正把探针的 `contype` 设为 0，保留其食物 affinity。食物自己的 type 8 仍能触发口器接触；真实身体与地面、墙和另一只身体继续正常碰撞。修正不加入自动掉头、目标追踪或神经状态重置，也不把探针排除后宣称为学会觅食。

本次修正在独立工作区完成；正在运行的封闭果园 attempt 2 继续使用原始源文件，保留为旧碰撞规则下的对照记录。旧记录不能用于断言修正后的行为，后续需要在相同设计、地图与种子下重新评估。

局部验证：19 项地形/真实物理接触/多模态输入与回放检查通过，1 项依赖完整图谱的测试未在独立工作区执行。测试覆盖移除探针的墙和对手接触，同时保留实际身体碰撞、食物口器接触及输入记录；完整仓库 CI 另行执行。

## 环境触觉增量：从实际碰撞到神经输入

新增可选 `engineered-multimodal-v2` 感觉配置。果蝇解剖身体与障碍物、另一只果蝇发生的 MuJoCo 接触，会和口器食物接触一起生成二值触觉输入；普通地面支撑与透明摄食探针不属于环境接触。v1 保留原来的食物触觉语义，便于同条件对照。

回放记录实际接触对象、独立的 `environment_contact` 事件、送入触觉神经群的数值，以及该群在神经推进后的平均活动。网页区分环境接触与食物接触，可以从事件跳到同一身体/脑活动时刻。这个版本仍用明确标注的工程电流刺激 `mechanosensory_tactile` 群，不推断受体、侧别或生理校准，也不加入自动避障动作。输入进入网络不等于已学会避障或持续觅食。

验证采用实际 MuJoCo 墙壁、对手、地面和口器接触，并用小型明确标注为测试夹具的网络跑感觉→神经→物理→回放流程。完整连接组的新行为效果仍需单独实验，不用测试夹具替代科研展示样本。


最新执行：环境触觉增量已提交正式 PR #54（非 draft），本地相关物理/感觉测试与节点测试通过，完整 GitHub CI 正在运行。原完整连接组实验最近进度为 75%；碰撞修复后的 5 秒对照沿用已排队的同一比赛，等待全局计算锁。两者仍使用各自冻结的旧代码，环境触觉增量未混入实验。

## 完整事件导航与按实际采样跳转

生命事件面板不再截取最后 12 条记录。现在按时间排序保留所选个体的全部事件，支持食物/感知、身体接触、越界分类与逐页查看，并可定位当前播放时刻附近的事件。早期食物接触和摄取不会因后续反复碰撞而消失。

点击事件后，身体时间轴和脑响应窗口共同选择事件后的首个实际记录帧；使用物理 tick 判定先后，不再假设所有回放都是 100 Hz 或固定加 0.01 秒。测试覆盖 20 Hz、100 Hz、不规则采样以及终点没有后续帧的情形。此增量改善已有与后续回放的可观察性，不将界面导航测试当作觅食行为证据。
