# Contact embodiment v7: physical transduction passed, causal contact failed

Scientific verdict: **partial**. The single approved candidate was built and physically executed. This did not qualify improved free walking. The frozen causal stop prevented the dependent official/submitted comparison and free-body pilot. No gain, model, selector, contact fixture, adhesion or coordinate search followed that failure. The complete original goal remains active.

The isolated checkout is `/tmp/fly-arena-embodied-v7`, branch `research/contact-embodiment-v7`, based on `61a89f6cb94e4a6ff206f3fe1e3a147c535a44b2`. The original interpreter, data, and three existing artifacts were read by explicit paths. `scripts/env-v7.sh` pins the isolated source and caches and disables bytecode writes. Original source, Git, DB, services, data, old scientific evidence and gates were not changed. No installs, subagents, commits, pushes, remote operations or external activation occurred.

## Executed stages

| Stage | Outcome | Actual evidence |
|---|---|---|
| 1. Composed input and bounded neural motor authority | PASS implemented/tested | Same 1,877 tactile afferents, 49 Ti MNs; full 165,122-neuron / 25,563,197-edge graph; immutable copied groups; complete current rebuild; atomic input/checkpoint validation |
| 2. Named physical joint/coordinate registration | PASS | Actual FlyGym 2.1.0 / MuJoCo 3.9.0; all 12 prescribed ±0.05 rad pulses agree with geometry-derived anatomical sign |
| 3. Archived neural rates → real physics | PASS restricted transduction | All 16 × 0.6 s intact/zero-output continuations completed; RF null retained |
| 4. Actual physical contact → full graph → joints | FAIL causal gate | All 48 × 1 s WT scientific trials plus one exact LF integrity repeat completed; 0 observed tactile-afferent spikes; tactile clamp removes 0% of neutral-context increment |
| 5. Official Nectar / submitted Preview physical comparison | NOT RUN | Required preceding causal gate failed; real artifact identities verified and displayed without fabricated comparison traces |
| 6. Free-body development pilot | NOT RUN | Required preceding causal gate failed |
| 7. Heldout free-body and original behavior admission | NOT RUN | Dependencies failed; original fifteen gates remain unchanged and unqualified |
| 8. Verification/viewer | PASS numerical integrity; UI visual limit | 19 tests; independent gate recomputation; exact saved physical and full-neural interval replay; self-contained HTML/SVG from recorded arrays |

There are 77 physical records: 12 coordinate pulses, 16 archived-neural body replays, 48 scientific contact trials and one deterministic contact repeat. Total simulated body time is 65.8 s. New-condition neural integration is 48 s; the exact repeat and two archived interval checks are separately identified verification work.

## Physical interpretation and measured results

The thorax is a mocap tether. Sixty non-Ti joint coordinates have explicit equality constraints at NEUTRAL (`solref=[0.0002,1]`, `solimp=[0.9999,0.9999,0.001,0.5,2]`). Six named trochanterfemur→tibia PITCH coordinates are exposed through existing position actuators, kp=45 and force range [-65,65]. There is no floor, CPG, Hybrid reflex or adhesion actuator. This fixture cannot stand in for locomotion. Maximum recorded non-Ti constraint error was 0.000017037258790486565 rad across the 49 contact records (10 ms sampling). Across all 77 physical records it was 0.0021372277939230994 rad, reached during a coordinate pulse (0.1 ms sampling). Constraints are numerically stiff, not mathematically exact. These are maxima at saved frames, not bounds on unrecorded inter-frame behavior.

Geometry-derived signs are +1 for every knee: increasing its named coordinate decreases the included femur–tibia angle, with finite-difference derivative approximately -1. No array offset was assumed; named qpos addresses are 5,16,27,38,49,60. Actual pulse response confirmed each sign once in each direction.

The only new readout is `.1*(mean flexor Hz - mean extensor Hz)/Rmax`, with `Rmax=444.41470981063657 Hz` from the fixed LIF rate filter and 23-step minimum spike interval. All annotated MNs remain in arithmetic-mean pools. This is an engineering transfer, not fitted physiology. Negative rates or rates above the model bound are rejected; there is no output clipping. The highest recorded individual rate across all 49 contact records was 444.41470830642857 Hz, within the ceiling.

| Knee | Archived-rate replay peak own active-minus-zero rad | Consecutive sampled duration above .01 rad (s) | Neutral-odor contact peak active-minus-motorzero rad |
|---|---:|---:|---:|
| LF | .0151174826 | .1493 | .0044341077 |
| LM | .0524413393 | .2721 | .0252336664 |
| LH | .0154440855 | .1594 | .0079132919 |
| RF | 0 | 0 | 2.0872e-14 (numerical zero) |
| RM | .0444150769 | .2586 | .0189497442 |
| RH | .0311285361 | .2395 | .0029436970 |

Archived-rate LF repeat maximum error is 0 rad. Zero-output settled RMS knee error over .2–.6 s is .0007960628 rad, below .001. Replay durations use consecutive sample count × .0001 s, with endpoint quantization of one timestep; all required margins exceed 50 ms substantially. These are actual body motions driven by recorded neural rates, explicitly **open-loop replay**.

The closed-loop fixture uses a sphere of radius .10 mm tangent to the distal tibia at 80% of the knee-to-tarsus1 segment, moved .05 mm inward during .2–.3 s, held to .4 s and withdrawn by .5 s. The normal comes from static mesh geometry; a deterministic geometric distance root registers the compiled convex collision surface before any contact trial. The resulting tangent residual is <4e-16 mm. Convex offsets of .10036–.10485 mm are coordinate registration, not outcome-based behavioral adjustment. Seeds42/43 mean explicitly assigned ±.005 rad own-knee initial perturbations, not neural randomness.

Actual named-fly mass × compiled gravity is 10.0484811 native force units. Native gravity is [0,0,-9810] mm/s². Sensor force/weight ratios use the actual FlyGym API for tibia/tarsus1/tarsus2, including probe contact; no SI conversion is inserted. Peak own contact ratios in the 12 seed42 intact trials range .00423–.00776; the maximum saved tactile-afferent external input in that scope is 0.3725257161390243 mV. Across all 49 contact records the maximum is 2.7779656148103524 mV, reached by RF seed43 at saved tick100 (0.01 s), whose input was sampled from the initial contact at tick0. Seed43 initial contact is therefore included explicitly; the seed42 value is not a panel-wide bound. These measured contacts did not recruit spikes in the selected tactile afferents in **any** of the 49 closed-loop records. Odor-off produced zero neural spikes and exactly zero active-minus-passive knee increment. Neutral .55/.55 odor produced 1,648,134 whole-graph spikes per trial and moved some knees.

All 12 matched seed42 intact/tactile-zero pairs have bitwise-equal saved selected rates and spike counts, afferent/MN and whole-graph aggregate spike counts, pool rates, held commands, knee/qpos/velocity/force/work/contact traces, and saved physical integration checkpoints. This equality is limited to the explicitly compared fields and sample/checkpoint times in the corrected report. Tactile external-current samples differ. Complete neural state is not identical: the 36 matched neural checkpoint pairs at ticks2000/5000/10000 show a maximum voltage difference of 0.09369196868231455 mV and a maximum external-current difference of 0.07043891864189389 mV, both in LM/neutral at tick5000. At that checkpoint, 378 tactile-afferent voltages differ; differences in voltage and external current across the checked pairs are confined to tactile-afferent neurons. Checkpoint rates, synaptic current, refractory state, delay buffers, ticks, total spikes and held commands match bitwise. The voltage difference is a sampled checkpoint maximum, not a full-trajectory maximum. The sampled external-current difference reaches 0.3725257161390243 mV across the 12 pairs. Thus the neutral-context movements cannot be attributed to the local tactile path. RF own MN output remained absent.

The scientific contact gate failed for every leg in both contexts. Large neutral-context LM/RM motion does not rescue the causal failure: it precedes probe recruitment and persists unchanged under tactile clamping. No dependent free walking or ABC trial was run. Contact and motor controls, passive mechanical deflections, neural nulls, impulse/work proxies and all failures remain in the records.

## Post-run reporting correction

Only the current reporter, this document and new reporting tests changed. Scientific sources, protocols, dynamics, fixtures, original gates, frozen source snapshots, `var/embodied-v7/measurements.json`, attribution and inventory remain unchanged. The corrected report is a separate derived artifact at `var/reporting-correction-v7/measurements.json`; it records the original inventory/delivery hashes, verifies their frozen evidence and scientific source bindings, and records current reporter/docs/test hashes explicitly as **post-run** digests. `before.json` records the correction boundary before any edits. No new neural or physical experiment was performed.

| Saved-record scope | Records | Maximum absolute recorded Ti actuator force (native) | Maximum non-Ti constraint error (rad) |
|---|---:|---:|---:|
| Coordinate pulses | 12 | 2.2871832808675805 | 0.0021372277939230994 |
| Archived-neural body replays | 16 | 0.40978231159036227 | 0.00029677523922223836 |
| Contact panel, including integrity repeat | 49 | 0.22499999999999432 | 0.000017037258790486565 |
| All physical records | 77 | 2.2871832808675805 | 0.0021372277939230994 |

Force arrays record six named Ti position actuators; the all-record maximum does not claim coverage of all 42 model actuators. Constraint errors cover all 60 non-Ti coordinates relative to the frozen neutral state. The derived report provides a source record, tick and selected-array index for every maximum, and field-by-field comparisons for all 12 seed42 clamp pairs. No seed43 clamp comparison exists. Zero selected afferent spikes across all 49 contact records and the failed causal gate remain unchanged; subthreshold differences do not establish successful tactile recruitment.

Reproduce only the report and reporting checks with isolated caches (the original `env-v7.sh` points its caches into frozen evidence and must not be used for this correction):

```sh
export PYTHONDONTWRITEBYTECODE=1
export PYTHONPATH=/tmp/fly-arena-embodied-v7/src
export NUMBA_CACHE_DIR=/tmp/fly-v7-report-fix/numba
export MPLCONFIGDIR=/tmp/fly-v7-report-fix/matplotlib
export XDG_CACHE_HOME=/tmp/fly-v7-report-fix/cache
/Users/lexa/Desktop/lexa/omega/fly-arena/.venv/bin/python -m pytest -q -o cache_dir=/tmp/fly-v7-report-fix/pytest-cache --basetemp=/tmp/fly-v7-report-fix/pytest-tmp tests/test_reporting_correction_v7.py tests/test_embodied_v7.py tests/test_backend.py tests/test_architecture.py
/Users/lexa/Desktop/lexa/omega/fly-arena/.venv/bin/python scripts/summarize_embodied_v7.py --output /tmp/fly-v7-report-fix/recomputed-report
```

The reporter is read-only on import and rejects output inside frozen primary evidence, including symlink aliases. It refuses to overwrite an existing corrected measurements file. Its provenance links the preserved original attribution; it does not regenerate attribution or consult prior-worker scratch. Scientific verdict remains **partial: restricted transduction PASS, contact causality FAIL; ABC and walking NOT RUN**. Browser visual QA remains unavailable. The correction checks passed: 23 tests in 4.07 s (the unchanged 19 targeted tests plus four new reporting regressions). Independent terminal review and publication belong to the root workflow.

## Records and verification

Primary artifacts under `var/embodied-v7/`:

- `replay/registration.json`, `geometry.json`, `compiled.mjb`, `compiled.xml`, coordinate and replay arrays/checkpoints, `gate.json`.
- `contact/registration.json`, exact subject bindings, annotation rows/groups, common rest, all 49 condition records and 147 complete neural checkpoints, `gate.json`.
- `verification.json`: independently recomputed gates and zero-error checkpoint continuations. All 28 coordinate/replay body records and 49 contact body records reproduce the saved .2–.21 s physical interval exactly. Full graph state and interval spikes reproduce bitwise for archived .2–.5 s LF/off and RM/neutral continuations.
- `measurements.json`: frozen original post-run summary, retained unchanged. Its broad neural/body equality label and unqualified maxima are superseded by `var/reporting-correction-v7/measurements.json`; do not use those old fields as full-state or all-record claims.
- `viewer.html`: read-only standalone selector with actual six-knee, passive-subtracted, actuator-force, contact, MN-rate and afferent-spike plots. It has no external dependencies. Official Nectar and submitted Preview are shown by exact supplied identities, with explicit NOT RUN coverage; submitted ownership is not presented as human authorship or current-session ownership.
- `joint-phenotype.svg`: standalone scientific summary of actual contact body curves.
- `attribution.json`: exact hash of the unchanged v6 helper from its frozen assay source, plus supporting installed source/mesh/config hashes. Scientific source/geometry/compiled binary were frozen before runs; supplementary asset hashes were collected after runs and are labeled accordingly.
- `deviation.json`: the single predeclared counting deviation below.

Historical execution commands from the isolated checkout (preserved for provenance, not rerun for this correction):

```sh
sh scripts/env-v7.sh -m pytest -q tests/test_embodied_v7.py tests/test_backend.py tests/test_architecture.py
sh scripts/env-v7.sh scripts/replay_embodied_v7.py
sh scripts/env-v7.sh scripts/contact_embodied_v7.py
sh scripts/env-v7.sh scripts/verify_embodied_v7.py
sh scripts/env-v7.sh scripts/viewer_embodied_v7.py
node scripts/check_viewer_v7.cjs
```

The scientific emitters refuse an existing output directory. Do not rerun them into these immutable records. `verify_embodied_v7.verify()` returns recomputed results without writing; its CLI writes only the derived verification report. The verifier independently derives readout/gates from arrays without importing candidate/readout/gate code; archived neural replay uses the unchanged baseline integrator, so it is not a second independent LIF implementation.

Tests passed: 19 in .58 s. Viewer JavaScript/data/finite SVG coordinates passed all 192 trial×knee selections in a minimal DOM. Browser visual QA could not execute because the available browser tool reported unsupported API-key authentication. Layout/rendering has therefore not been browser-verified; no server was started to work around that limit.

**Exact model replay must use `compiled.mjb`.** The actual MjSpec XML export round trip changes fused thorax mass by -2.052e-6 native units, rounds equality constants by up to 4.5303e-6 rad and fills unused equality slots differently. The binary, registered source and recorded compiled arrays are authoritative. The exported MJCF is inspected and preserved but is not represented as losslessly recompilable. Existing original scientific records are unchanged.

Neural/body clocks are both .1 ms with 10 ms sensory/command intervals. Each body's interval uses the prior held command; neural input sampled at its start produces the next held command. Checkpoints contain full MuJoCo integration state, body tick and applied command plus complete LIF state and next held command. No world/target/reward/heading enters motor calculation. Input composition uses the unchanged research odor encoder once, together with all six tactile groups.

Force impulse is a 10 ms force/weight quadrature multiplied by registered native weight; work is actuator torque×angular-velocity integration, a mechanical proxy. Contact onset/spike onset summaries have 10 ms resolution. Seed43 sign checks use pre-contact-referenced excursion because the approved plan supplies no seed43 passive panel. This limitation does not affect the observed causal rejection. The bounded evidence is approximately 352 MiB before final source/manifest files, under the registered 700 MB contact-record bound.

## Predeclared deviation and unresolved full goal

The explicit WT factorial already totals48, while the same approved step also demands an exact LF repeat. Before contact execution, `deviation.json` declared **48 scientific trials plus one identical-condition deterministic integrity replay**. No new candidate, condition, neural parameters or gains were added. Initial API preflight fixes (native equality `data`, MjSpec XML exporter) occurred before any scientific registration or pulse; their failed setup artifacts are preserved.

Free walking, olfactory approach, capture/retention/fork choice, meaningful matched design strategy differences, original fifteen-gate admission, richer modalities/memory/social behaviors, device/remote/UI validation and reviewed source publication remain unresolved. Existing provenance/platform/Chrono ports and inactive NyxID remain baseline features, not newly qualified outcomes of this fixture. No production admission or original-source synchronization was performed by this isolated worker.
