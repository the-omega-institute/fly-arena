# V12 retained-data validation repair — rejected at review

The v2 contract repair passed its final 50-test selection and processed all 32 retained trials, but all three independent reviews rejected engineering acceptance. The authentic retained payload is consistent and the negative recovery finding is reproducible. This is not coordinated walking, neural, phenotype, or product qualification.

## Confirmed remaining defects

- Direct producer and verifier partition helpers can miss a nonpositive phase increment crossing a membership boundary, despite the full-window entry points detecting it.
- Equal malformed nested cache digest records and non-hex controller digest strings can pass restoration digest validation.
- Registration validators do not enforce the exact three source/snapshot identities and immutable v2 policy fields.
- Finalization can inventory substituted receipts, stale registration links, false JUnit hashes/counts, incomplete source seals, and altered legacy-input declarations. Byte inventory integrity does not establish receipt semantics.

These are demonstrated rejection-boundary defects. The actual sealed files have the intended identities and valid schemas; no retained-data or motion-gate inconsistency was found. The prior rejected v1 remains ineligible for future positive admission.

## Reproduced evidence

The tests reviewer independently reran both retained-data producer and verifier successfully: 32 trials, 28 active and 4 zero controls, 3,192,000 active phase increments, 5,008 complete active phase runs, 1,280,000 endpoint/next-cache rows, 2,768 stance-slip formulas, 9,095,759 raw normal-force rows, and 100 restoration ticks. The architecture reviewer independently reproduced the verifier result. Each reviewer also ran the focused 50-test selection; the unchanged physical fixture was excluded.

Separate adversarial checks reproduced the remaining defects. The tests reviewer retained a finalizer suite with one passing source-tamper control and four failures because forbidden substitutions were accepted. Existing passing tests therefore do not justify acceptance.

The root independently matched 12,338 original index files, 150 old correction files, 164 published source files, 81 legacy input bindings, the three new source files, all four retained JUnits and all closed output files. Contract-01 contains 30 payload files plus its self-excluded inventory: 980,134 bytes total. Inventory SHA-256: `b52f8b8853e71b2098b5157b61eb1f401073eea238493de8b8ca77083df142cc`.

No physical or neural integration was performed by this repair or its reviews. Geometry/velocity coverage remains accurately scoped: this is retained-array validation, not a new exhaustive Jacobian or wrench reconstruction. One architecture-review collect-only command accidentally updated pre-existing bytecode caches before applying the required cache environment; reviewed source and sealed evidence hashes were unchanged. Those cache bytes were not claimed to be restored.

## Preserved artifacts and next step

Local delivery: `var/delivery/behavior-v12-validation/`. Its `validation-payload.tar.gz` contains 31 verified regular files, 182,767 bytes, SHA-256 `26dc12ebcb3482a2dd70b438ebfce0e63ca1a336da42cf0058f4e7fa1b8af612`. Large historical experiment archives remain local and are not claimed to be uploaded to GitHub.

Preserve contract-01 and these rejected source identities. The next targeted correction must close the four demonstrated boundary defects under a new identity and full independent review. After acceptance, the next substantive scientific task must return to actual walking, then held-out/repeat/restore, original neural15, matched WT/official/user phenotypes and ablations, and Lab/API/replay integration. No service deployment or public activation follows from this report.
