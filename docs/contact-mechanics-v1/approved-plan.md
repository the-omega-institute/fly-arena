# Contact mechanics v1 — one converged native integration and execution plan

Status: implement source, review, then execute the finite registered native experiment if the review and preflight predicates pass. This is the one allowed meta convergence. No clarification pass. Root approval of this plan authorizes its bounded implementation and, after source acceptance plus prospective native registration, its named local preflight and fixed mechanical panel. No further user permission is needed for those local tasks already within the user objective. It does not authorize deployment, NyxID activation, GPU staging, a new controller, or additional scientific retries.

## Fixed goal and decision

The full user goal in goal.json remains unchanged. This component must produce actual fair mechanical evidence for immutable PR20 acquire-lift-transfer-v1 against fresh unchanged v16; a source-only runner is an intermediate deliverable. All three terminal thinkers propose that path. Their designated plans are retained at var/delivery/contact-mechanics-v1-planning/{minimal.json,structural.md,delete.json}. This document controls any difference between them; do not blend alternative preflights or count allowances.

Selected architecture: the minimal proposal's explicit profile/runtime/schema seams, structural's exact realized event partition and clock/checkpoint ownership, and delete's reduced static preflight and shared traversal with independent arithmetic. No model/controller/native intent preflight proposals, synthetic trajectory, 288-basis sweep, four separate initial restores, eight doubled reset builds, duplicate full-panel geometry traversal or legacy-schema padding.

The known-good frame is a prospective physical experiment with separately owned physical state, completed intervals, native intent and realized controller state; independently calculated physical evidence decides admission.

```
[full goal: actual gait -> causal WT/official/design phenotypes]
  depends-on -> [G1 correct native dispatch/reset]
  depends-on -> [G2 interval/action/endpoint clocks and restoration]
  depends-on -> [G3 primary intent + lawful realized windows]
  depends-on -> [G4 retained physical/controller evidence + independent checks]
  depends-on -> [G5 finite registration/resources/failures + actual panel]
[minimal] agree -> [G1,G2,G4,G5]
[structural] agree -> [G1,G2,G4,G5]
[delete] agree -> [G1,G2,G4,G5]
[minimal direct phi substitution] conflict -> [G3 legal phase holds]
[G3 legal phase holds] resolved-by -> [structural explicit event/mode partition]
[minimal 8 builds/4 proposals; structural 4 builds/28 proposals/basis sweep]
  conflict -> [delete no intent preflight + 4 builds + 1 initial restore]
[preflight scope conflict] resolved-by -> [fixed reduced static preflight below]
[minimal/structural duplicate geometry passes] conflict -> [delete shared traversal]
[verification work conflict] resolved-by -> [single traversal, independent arithmetic/read checks below]
[G1,G2,G3,G4,G5] converges-to -> [concrete plan in this document]
[concrete plan] depends-on -> [source triplet acceptance -> finite native preflight -> fixed panel]
[actual panel + independent mechanical review] depends-on -> [later original259s downstream]
```

No unresolved design conflict is carried into implementation. Unknown actual API compatibility and cost have finite registered tests; they are not new diagnosis questions. Failure of those tests is retained as failure.

## Stage and worker boundaries

1. Delegate source implementation in /tmp/fly-arena-contact-mechanics-v1, starting from exact accepted PR20 source. Preserve all195 accepted files (including all180 baseline files) byte-for-byte. Only add integration/runner/schema/analyzer/verifier/tests/docs. Use the original local .venv. No git/network/install/service/GPU/raw old scientific evidence or opaque log reads.
2. Source implementation and its authored fixtures use zero actual native/model/kinematic/CPG/accepted advance_intent/neural/physics/graph calls. Native imports must be guarded during fixtures. Ordinary source/AST/array/schema/stream/phase-event/independent arithmetic fixture work is allowed. Return a complete executable runner and independently usable verifier, not another prospective facade. 75 minutes, one numeric thread, 1GiB observed RSS, 256MiB new source/fixture scratch. Record the exact source closure, fixture report, all original source equality, CLI contracts and the finite leaf-call table. Report a deviation before acting on it. No self-review.
3. Run independent architecture/quality/tests source reviews against the complete plan and implementation; each 30 minutes, one numeric thread, 1GiB observed RSS, 128MiB scratch, no science. Any reject blocks execution. This task has at most one source correction plus a complete independent correction triplet; no science retry is granted by that allowance. All-comment handling follows SSHX's bounded rule.
4. After accepted source, root registers a new execution flight before dispatch with precise code/model asset locators and leaf-call counts. The delegated worker performs the bounded local preflight, seals the final scientific registration, executes actual fixed trials and independent numerical verification if feasible. All science, including preflight, uses one 4-hour attempt and caps below. No code changes after preflight source freeze, no automatic repair/reprobe/rerun. The execution worker is not the independent reviewer of the resulting evidence.
5. Complete independent mechanical evidence review after terminal execution, using retained evidence under explicit read/compute caps. It may verify the already retained data; no extra physical trajectories. A negative/incomplete result remains negative/incomplete, ready=false. Do not close the full user goal.

## Runtime and profile seam

Use a small contact_mechanics_v1 package (runtime, registration/evidence schema, analyze, verify) and one CLI with source-check/preregister/preflight/run/analyze/verify entry points. Reuse NumericEvidence and existing independent prefix/failure finalization. Do not create a second generic framework or copy old full runner wholesale.

CONTROL construction/action is exactly mechanical_v16.new_body(seed, mechanical_v16.PROFILE) and mechanical_v16.step. mechanical_v16.CONTROL names old v15 and is forbidden as the new control. Candidate starts from that same factory/reset/neutral action but only accepted Controller/NativePort generates actions. Take untouched initial native phases, zero magnitudes, MT19937 state and preprogrammed_steps as its initialization. Use the accepted adapter's anatomical/output index map to select neutral angles from ctrl, exact neutral adhesion, and no guessed reshape. Old controller/sensor may remain as reset provenance but cannot be stepped or presented as candidate state. No Bodies.step or unadapted old NativeRecorder.row/state_finite/checkpoint/run/freeze/analyze_panel entry points for candidate.

Register nq=73,nv=72,nu=48 as the expected accepted model; a mismatch is an identity/integration stop, not dynamic schema resizing. Keep both original MJB SHA c84909b93a562b21c045466499187b964af92530c88ac9e627867a689d7819d2 and independently computed accepted adapter model_digest with their separate meanings. Resolve the original MJB path from compact immutable registration before the first native call; allowlist that exact model asset for comparison. No graph, GOLDEN trace or prior GiB trajectory scan. Preserve gain45/force65/adhesion40, mesh/model/reset/intent parameters and all prior gates.

At row0 retain physical reset and explicit no-completed-interval. At action k use u=[c(1-a),c(1+a)] only for3000<=k<25000. Candidate prepare consumes the recorded prior interval(k-1)->k and current endpoint q[k],v[k]; apply commits all legs. Set body.drives[0]=u. Independently copy prestate through record_pre_step immediately before exactly one sim.step; charge attempted physics before invocation, increment body.tick once on success, capture_completed then record_completed immediately before any authoritative cache refresh. No authoritative mj_forward inside trial loops. Output rowk+1 stores endpoint q/v and actionk/input; active observation_tick is k. Silence holds controller/sensor/diagnostics/RNG/ctrl exactly while physical recorder progresses. Control has the same passive pre/post evidence hook around its one unchanged step, with no candidate feedback.

Safety retains original finite qpos/qvel/ctrl/force/qacc/qfrc tests and |qvel|<=1e6; adapt candidate state inspection explicitly. Upright<=.8 remains metric failure, not newly introduced unsafe termination. data.time must agree with integer tick*dt at existing time tolerance.

## Finite static preflight

Freeze source/dependency/native library/asset hashes, scene/reset/input specs, schemas, plan, leaf-call table and resource limits into an exclusive preflight registration before scientific imports/builds. Four fresh v16 PROFILE body builds, one per seed42,43,31042,31043. For each, bind candidate and verify candidate conversion preserves physical reset/neutral action; retain exact q/v/ctrl/integration/native phases/zero magnitudes/RNG and model identities. The common unchanged reset path does not need a duplicate body per profile.

Use delete plan's reduced static work: at seed42 only, two detached preparations of the actual reset q/v for independent geometry/velocity comparison, one all30-minimum/six-distal-centroid/thorax/COM/Jacobian primitive check, and six native excursion calls (12 detached poses), using an expressly static observation representation. It is not a completed physical packet and must never be passed to prepare/propose/apply or scored as a gait interval. No accepted advance_intent, Controller.propose, native CPG step, physics, fictitious phase advance or other seed static sweep. At each seed at most one neutral reference conversion. At seed42 take one genuine initial NativePort checkpoint and one same-state full native restore; validate complete integration/cache/controller/sensor/recorder identity and numeric checkpoint export. No extra restore corruption probes. All constructor-internal forwards/keyframe/controller resets are counted.

The implementation must expose an explicit callable static helper assembling these existing native primitives without manufacturing a completed interval. Exact per-primitive counts must be generated from the fixed call graph and reviewed before native registration. If an API cannot be bound without a missing contract, return the exact source gap; do not hide an extra scientific call in setup.

Preflight wall subcap600s, bytes128MiB, included in the overall4h/8GiB caps. API/model/cache/schema/finite/resource mismatch ends the attempt with exact retained receipt; no repair/reprobe. Successful preflight seals an exclusive final registration including all exact measured identities and its literal hash in execution-start before step1. Every actual fresh trial reset must match its seed's frozen snapshot. Check source and input closure before each trial without scanning unrelated data.

## Evidence and independent calculations

Retain common stride1 physical stream: qpos/qvel, ctrl48, actuator_force48, input2, native theta/r6, tick/time, observation/action tick, explicit interval presence/count. One profile-specific stream holds exact legacy v16 diagnostics for control or candidate realized state. Candidate includes phi/modes/resume/cycle/partial, all clear/load/distal/wait counters, last_tick/generation/z0, AP/lateral/release height/magnitude/excursion/transfer_start/landing_xy, native reference and correction42, load totals, shared twist/capture/release/veto flags, solve z and residual summaries plus requested/limited/before/after-ball/range outputs. Use only actual accepted data; unsupported old reflex fields are absent and explicitly mapped. Immutable config/RNG bytes stored once; mutations if any recorded exactly.

Keep every owned contact in native order, including nonfoot, nonpositive force and positive margin: interval endpoints, contact index, geom/ground/leg, signed distance, point3/frame9/wrench6 and ownership. Prior q/v links to preceding core row after exact independent saved-prestate equality. No truncation at512, decimation, peak-only substitution or target-derived geometry. Capture full failing state including qacc/qfrc when unsafe.

Use exclusive1000-row NumericEvidence chunks, explicit float64/int64 schema, lossless bytes and ragged contact offsets. Do not duplicate q/v, dump full per-tick JSON/Jacobians/A/b or entire mesh vertex arrays. Adjacent q/v + raw packets + fixed assets + complete compact previous/current controller state must suffice to reconstruct each task and command; retain full state/task payload at initialization, source checkpoint, rejected action and first verification mismatch. Store residual summaries and projection vectors; numerical tolerance comparisons, not exact floating digests alone, determine independent arithmetic agreement.

One retained-data traversal per trial/chunk with two rolling detached workspaces reconstructs endpoint geometry and coherent material velocity. Within that traversal use separate producer and verifier arithmetic/read checks: object-point velocity versus independently formed point-Jacobian product; raw mesh vertices/transforms versus independently indexed minima/centroid reductions; source/prestate/contact identity and quadrature independently checked. Both calculations derive from actual retained q/v and model, not the producer's derived stream as sole oracle. No second full native geometry pass is required. Preserve checked outputs and discrepancies for source/evidence reviewers.

For every candidate active action independently rebuild intent equations, native references, material tasks, load/dwell/phase transitions/release arbitration/body twist, six solves and rate/ball/range projection from retained prior state, q/v and correctly timed packet. Do not call candidate propose/advance_intent as oracle. Control uses actual v16 independent mathematics/schema, not v15. Reconstruct excursion only at recorded initial/release latch events. Charge independent algebra and native reconstruction work. Partial trials receive prefix checks and full missing-remainder status; never pad to40001rows. Two independently implemented cycle calculations and exact expected-ID/grid checks remain required.

## Original and realized metric partitions

Primary theta uses unchanged original _phase_window_contract/_cycle_metrics/_cycle_gate meanings, strict increasing intent, inclusive6000..25000, median31/no-padding, exact earliest PEP and later global AEP/ties/reversals/inconclusives/partials, all-foot unloaded>=30 intervals at height>.02mm, support>=30intervals, fullstance slip<.15mm. Preserve original upright/yaw/turn/speed-dose/stop formulas and5e-12 inherited numerical tolerance. Zero cases retain only the originally declared locomotion exemptions.

Additional realized partition uses structural's actual mode/event contract. Never feed held phi directly into the strict intent partition, change that helper, compress held ticks or reset cycles. Use actual cycle IDs and LIFT/TRANSFER/LAND swing versus SUPPORT/RELEASE_WAIT and appropriate REACQUIRE support events. Keep every legal held interval; require nondecreasing phi, valid exact mode/boundary holds, unchanged lag<=pi/2/wait<=500 and no skips/backwards/resets. Boundary partials/failed runs remain partial/inconclusive. Apply identical physical AP/extrema/dwell/fullstance-slip calculations to actual-time event runs. Source fixtures must discriminate legal holds, illegal holds/skips, and physical dwell/slip failure despite legal phase labels. Both candidate streams and exact ID sets feed admission; unchanged primary failure cannot be waived.

## Fixed panel, cross-seed checkpoint and failure policy

Development: seed42 then43; ordered zero,straight008,straight02,straight04,turnnegative04,turnpositive04,turnnegative08,turnpositive08; each control then candidate.32x40000ticks atdt.0001=128s. Complete all safe conditions despite ordinary metric failure; exact whitelisted accepted-law stall/lag/finite infeasibility stops that candidate before next step and retains its failed remainder, then continues safe development. Unknown ContractError, solver infrastructure error, identity/schema/clock/prestate/proposal/numerical mismatch, physical unsafe, resource or retention error stops the whole attempt. No held-control continuation/fallback after failed action. No missing condition removed from denominators.

Only complete candidate primary+realized+aggregate passes and independent verification/retention closure allow heldout candidate31042/31043x8=64s. Comparator physical failures are reported and cannot waive candidate gates; broken comparator integration invalidates the comparison. Only heldout pass allows repeat candidate31042straight02=4s; exact repeat against heldout must pass before restore.

Capture one accepted full native checkpoint after completed development candidate42straight02 poststep10000; controller/sensor observation normally9999 and recorder interval9999, physical endpoint10000. Retain reference ticks10001..10100 from that same scheduled trajectory. Keep copied MjData alive and export every numeric cache/integration/controller/sensor/recorder field. This tests live native continuation, not unproven crash-resumability.

At completed repeat destination40000, apply the existing documented no-physics corruption recipe to qpos[0]/qvel[0]/ctrl[0]/one controller phase/tick/drives, then exactly one NativePort.restore using experiment-wide binding and separately recorded source/destination trial identities. Restore seed42 scientific state intentionally; do not bind equality to destination seed31042. Continue exactly100ticks at[.2,.2]. Compare integration/full native cache/controller/sensor/recorder/action/raw contacts and independently derived physical data bitwise with reference. Ephemeral owner/revision and timing excluded; first mismatch retained. No new body/trajectory for restoration. If checkpoint absent due failure, restore stays blocked without stopping remaining safe development.

Max scheduled1,960,100physicalcalls=196.01s; hard2,600,000 is emergency only, never additional trial allowance. Neural0. Original downstream259s unchanged and separately gated after genuine mechanical review.

## Exact accounting and resources

One local process/numeric thread; explicit BLAS/OpenMP limits1 and guarded fixture imports. No GPU/remote/network/install. Native attempt4hwall inclusivepreflight/build/run/reconstruction/verification/finalization,2GiBobservedRSS,8GiBall source/evidence/scratch and20GiBfreefloor. Reserve128MiB and at least60s plus measured finalization cost. Preflight/source review doesn't claim OS-enforced isolation. Budget actual bytes plus pending uncompressed chunk/next allocation; do not rely on compression assumption or delete valid records to continue.

Top-level maxima:4preflight+49trial builds=53;4preflight+33candidate binds=37; one saved MJB +one verifier model load reused serially;2NativePort.checkpoint and2NativePort.restore calls (initial and conditional active). CPG constructor/reset internal caps conservatively8events/build=424, to be expanded by exact audited leaf call graph; no CPG advances during preflight. Ac<=638100candidate active runtime calls; Av<=308000control active CPG advances; candidate physical<=1320100, control<=640000. Count failed attempts before invoking each call. N<=1960100completed/attempted scheduledticks;R<=1960149endpoints;E<=512*N;B=frozen flybodycount.

Candidate observation:2kinematics/comPos states,36minimum/centroid+2Ccontact pointJacobians,1thorax andB COM Jacobians each active action;6solves and<=1sharedfit; TWO angles() references=12leg splines/action, plus2per excursion. Correct the lesser6Ac spline bound in the minimal/delete proposals. Excursion latch count may use conservative L<=6Ac+6, with actual registered fixed-rate tighter cap if source-proven before preflight; no implicit5800 allowance based on an unaudited claim. Geometry per type<=2Ac+2L plus explicitly static14; native spline<=12Ac+2L plus bounded initialization. Full independent task reconstruction has the same finite primitive ceilings on every active row; physical reconstruction<=Rkinematics/comPos/fwdVelocity plus actual per-contact object/Jacobian work and event excursion poses. Algebraic intent checks<=Ac+Av; do not confuse independent equation evaluation with a second production advance.

32mj_copyData/512mj_getState/512cache-manifest ceilings for checkpoint/reference/actual/failure work, separately expanded for internal calls.200 reference/actual samples plus two captured states; no ordinary per-tick full-cache hash. Registration must include a finite named allowlist/cap for constructor/native-library internals and static/checkpoint paths, with reserved attempted/completed categories. Instrument immutable call boundaries without editing scientific source; no unlisted model/native operation. Exact source-derived leaf ledger is a required source-review artifact BEFORE first native call, not an invitation to native exploration.

Measure only fixed preflight operations and actual first prescribed1000-step chunks/first active100-step prefix; all physical prefix stays evidence. Analyze/verify disjoint retained chunks exactly once; unknown cost is marked unknown, not zero. After each chunk project required remaining work using twice maximum measured cost/byte rates plus reserve. Mathematically loose maximum call capacities are hard guards, not presumed expected event counts; use fixed schedule rates and observed event costs for forecast with explicit uncertainty. If even known lower-work projection cannot fit, stop as resource-incomplete. Unknown active categories get measured only when the prescribed trial reaches them; no unregistered warmup. Check wall/RSS/freefloor every100ticks and before expensive calls/allocations/flushes. Any actual cap, unsafe allocation or projection stop retains its valid prefix and registered remainder.

Independent finalization attempts for each stream/snapshot/terminal preserve primary and secondary exceptions. All49scheduled IDs are registered beforehand with blocked conditional status; terminal report separates measured failure, integration error, resource stop and unstarted cases. No successful outcome or source test enables production. Keep ready=false.
