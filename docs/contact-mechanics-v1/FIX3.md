# FIX3 — transparent accounting keyword forwarding

The registered `execution-local-01` attempt stopped during its first seed-42 model build at `MjSpec.add_key(name="neutral", time=0)`. `Budget.call(self, name, fn, *args, **kwargs)` bound the forwarded native `name=` to its own accounting parameter and raised before charging or invoking that leaf. That attempt remains terminal, with zero physics and neural work. FIX3 is a separately authorized source-only repair; it neither edits nor retries the executed attempt.

The sole runtime change is the signature `Budget.call(self, name, fn, /, *args, **kwargs)`. Its three internal parameters are positional-only, allowing `name`, `fn` and `self` to pass unchanged inside the native callable's keyword dictionary. Source AST inspection confirms every integration caller already supplies the accounting category and callable as ordinary positional arguments. The body of `Budget.call` is byte-for-byte unchanged. No law, gate, schedule, capacity, native wrapper body or other runtime module changes.

The guarded focused fixture receipt has 12 passing tests: eight new forwarding regressions and four preserved accounting/allocation/ledger regressions. The new tests reproduce the old argument-binding failure before any charge or leaf invocation; verify positional payload, keyword identities and return identity; verify failed-call exception identity, attempted/completed counts and deterministic timing; reject capped, unregistered and preflight-dynamics work before leaf invocation; and exercise the actual native-guard context manager with authored Python-only dependency stand-ins, checking all patched attributes, the classmethod descriptor and active-budget restoration on both success and exceptional exit. These stand-ins establish Python forwarding and context cleanup only. They do not establish compatibility with real extension types or native model construction.

`run_fix3_fixtures.py` uses the original interpreter, one numeric thread, disabled bytecode, scientific import guards, execution-path profiling and forbidden-input audit hooks. Zero actual native/model/CPG/intent/neural/physics calls occurred. Native module names exist temporarily only as authored stand-ins within the guard tests and are restored afterward. No broad suite rerun, preflight, model payload read or scientific retry occurred.

The source ledger changes only the budget source hash and byte count; all 55 categories, capacities, derivations and 33 source-file entries remain equivalent. All 195 accepted source files remain exact. The prior FIX2 reports remain historical; `fix3-source-flight-report.json`, `fix3-fixture-report.json`, `fix3-fixtures.txt` and refreshed `source-closure.json` bind this handoff. The source-closure manifest excludes itself to avoid self-reference.

Independent source and failure-evidence reviews remain required. A later scientific attempt requires new prospective registration and an exclusive output root. This repair is not native validation, source self-approval, readiness or full-goal completion: `ready=false`, `full_goal_complete=false`.
