# FIX1 source correction map

This is the sole registered source correction after the initial architecture, quality and tests reviews rejected the runner. All changes are confined to the newly added integration package, fixtures, source ledger and documentation. The accepted195-file source, accepted controller laws, original physical thresholds,49-trial denominator,1,960,100 scheduled physics steps, build/bind/checkpoint/restore bounds and zero-neural budget are unchanged. `ready=false`; `full_goal_complete=false`. No native or scientific command was run and this document grants no review approval.

| Finding group | Implemented source correction | Guarded regression evidence |
|---|---|---|
| 1 / ARCH-1 | `control_evidence.py` exports all numeric controller/configuration state, exact CPG fields/RNG, output-order descriptors, spline numeric identity and asset/model/input binding. Runtime initialization and serialization are inside the retention boundary. | Old AST-extracted checkpoint rejects authored object-shaped state; new adapter round-trips numeric state and detects mutation/unknown objects. Factory and initial serialization failures produce honest terminals. |
| 2 / ARCH-2, T1 | `proof.py` requires an exact v2 seal with all six mandatory file hashes, four start hashes, initial-controller hash, true traversal marker, exact trial/context/coverage and complete child closure. It binds registration, preregistration, source, reset, plan, experiment, input and dependency identities, and verifies reset/initial state, clocks, domains and denominators. `registration.inventory` includes all195 accepted paths. | Empty/missing/false seals, missing hash, wrong coverage/context/start seed, changed core with unchanged derived, wrong time/fractional clocks, altered initial state/terminal/registration/plan/source/reset/panel ID reject. A coherent synthetic failed-prefix protocol is readable; an unsealed prefix remains unverified. |
| 3 / T2 | Every report, including comparator reports, passes exact ID/schema/completeness/integration/retention/closure and gate-consistency checks. Candidate admission requires consistent original/realized gates and full dose. | Five original stale/missing/false-evidence reproductions reject; wrong primary and realized evidence reject; valid full panel passes; ordinary comparator physical failure remains reportable; missing ID and failed dose reject. |
| 4 / Q1 | Realized events accept the lawful compound REACQUIRE(resume RELEASE_WAIT) to LIFT release with exact boundary/cycle/admission/dwell/wait prerequisites. Phase validity is separate from physical arithmetic agreement. | Accepted source-law proposals with authored observation/reference/intent reach the transition without editing controller state. Each prerequisite mutation rejects. A phase-invalid prefix retains matching physical calculations without an integration-disagreement exception. Existing illegal holds/skips/lag/wait behavior stays enforced. |
| 5 / Q2 | Trial finalization incorporates child completeness/retention failures and required snapshot secondary failures before terminal publication. Recovery metadata binds emergency bytes, orphan bytes and exact initialized prefix counts. | Injected final-flush errors with/without a primary exception produce explicit incomplete retention terminals, preserve errors and return exactly one checked emergency row. Corrupt emergency bytes and complete reads reject. Partial writer construction and snapshot failure are finalized. |
| 6 / Q3, T3 | Disjoint remaining-work categories and maximum measured rates are persisted and summed with elapsed time and measured finalization reserve. Runtime, reconstruction and conditional restoration consume remaining counts. Pending memory, disk and resident buffers are separate. MjData preallocation uses source-header dimension coefficients. | Two8000s remaining categories stop when combined;1.9GiB observed plus0.2GiB pending stops without allocation. Disk/buffer reserve is not counted as new memory. Unknown categories/maxima/reserve/stop receipt are checked. Authored MjData constructor is not invoked when its reservation exceeds memory. |
| 7 / T4 | Failed-prefix original and realized cycle calculations use the actual retained intersection with6000..25000; complete cycles and clipped boundary partials are preserved while the full40000-tick result stays failed/inconclusive. | A12001-row prefix retains both cycle classifications and partials, reports28000 missing ticks and never admits. Original scalar and independent helper mathematics are unchanged. |
| 8 / contact domain | Negative native contact indices reject; nonnegative sparse native order is preserved. | Negative, duplicate, fractional, reversed, wrong-interval and513-row packets reject; sparse2/7 zero-force rows pass; missing endpoint rejects. |
| 9 / ARCH-3 | ContactForce source bound includes raw contacts plus all control row0/endpoint and active pre-action readers. The observation classmethod is instrumented and capacity-checked before its contact-force leaves. | Authored wrapper counts row0, active pre-action, endpoint and raw-contact families;513 owned contacts stop before observation. Generated55-category ledger values and33 source hashes match code. The92 native-data array coefficients match the frozen header source. |

The77-test final guarded suite is in `tests/contact_mechanics_v1/{test_source,test_fix1}.py`; run it only through `run_source_fixtures.py`. It uses the original interpreter, disables bytecode/plugin autoload and uses one numeric thread. The29 tests adapted from the explicitly designated reviewer source fixtures now assert the corrected rejection expectations. No reviewer opaque diagnostic reference was opened, read or hashed.

The control evidence adapter is an audit representation and makes no portable-native-restore claim. Synthetic source-only seals establish protocol behavior, not authenticity or physical correctness. The standalone saved review depends on the bound producer attestation of the one registered native traversal and never schedules another traversal.

`leaf-call-table.json` derives `mj_contactForce = E +512*(CONTROL_ENDPOINTS+Av)`, where `E=512*N`, `CONTROL_ENDPOINTS=16*40001`, and `Av=308000`. The factory and failure-export paths do not call `HybridControllerObservation.from_sim`; inactive control actions do not call it in `mechanical_v16.step`, while every retained endpoint does in `legacy_state`. The unchanged Bodies.step and old NativeRecorder.row are outside this dispatch. No extra physical or trial allowance was added.

The memory estimate for MjData uses all92 main-buffer arrays from the installed MuJoCo3.9 header. Each element is conservatively charged8 bytes, each array64 bytes alignment, plus the model arena and1MiB for the fixed struct/wrapper, then doubled. Native plugins are rejected before allocation. Native API/class-wrapper compatibility and actual allocation/costs remain unmeasured; source tests do not grant execution authority.

The full independent correction review triplet remains required. Native preflight, mechanical efficacy, cross-seed native restoration, downstream causal evidence and the user's complete Fly Arena goal remain outstanding.
