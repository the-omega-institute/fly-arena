# Contact mechanics v1 executable source handoff

Current source includes the explicitly user-authorized FIX2 report-consistency guard. See `FIX2.md` and `fix2-source-flight-report.json` for its scope, 14 targeted guarded tests and current hashes. The FIX1 validation history below remains historical; the previous one-correction stop was superseded only for this registered minimal FIX2. Independent FIX2 reviews remain required before scientific authority.

This is a source implementation for the approved plan, not mechanical evidence or a production admission. `ready` remains false. All 195 files in the accepted PR20 source manifest are preserved byte for byte. The accepted factory, v16 action law, candidate law, adapter and original metric/verifier files are unchanged. New code lives in `src/flyarena/experiments/contact_mechanics_v1`, with authored fixtures in `tests/contact_mechanics_v1` and the source ledger generator in `scripts/contact-mechanics-v1`.

## Commands and registration boundary

Run from `/tmp/fly-arena-contact-mechanics-v1` with the original interpreter. No installation is needed:

```sh
export PYTHONPATH=/tmp/fly-arena-contact-mechanics-v1/src
export PYTHONDONTWRITEBYTECODE=1
export OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1
export VECLIB_MAXIMUM_THREADS=1 NUMEXPR_NUM_THREADS=1
PY=/Users/lexa/Desktop/lexa/omega/fly-arena/.venv/bin/python
"$PY" -m flyarena.experiments.contact_mechanics_v1 source-check
"$PY" tests/contact_mechanics_v1/run_source_fixtures.py
"$PY" scripts/contact-mechanics-v1/build_call_table.py
```

The source-only flight executed only the guarded fixtures and static source ledger generation. The following commands are for the separately registered, source-reviewed scientific flight. `RUN_ROOT` must be a new exclusive local directory. Supply literal returned hashes; the CLI never infers an authorization hash or retries a failed operation.

```sh
"$PY" -m flyarena.experiments.contact_mechanics_v1 preregister --root "$RUN_ROOT"
"$PY" -m flyarena.experiments.contact_mechanics_v1 preflight --root "$RUN_ROOT" --anchor LITERAL_PREREGISTRATION_SHA256
"$PY" -m flyarena.experiments.contact_mechanics_v1 run --root "$RUN_ROOT" --anchor LITERAL_FINAL_REGISTRATION_SHA256
"$PY" -m flyarena.experiments.contact_mechanics_v1 analyze --root "$RUN_ROOT"
"$PY" -m flyarena.experiments.contact_mechanics_v1 verify --root "$RUN_ROOT"
```

`preregister` freezes source, dependency/native library/assets, interpreter binary/version, plan, model locator, all49 trial IDs, schemas and55 finite call categories before scientific imports. `preflight` validates that closure and anchor, performs exactly four registered factory builds and bindings, seed42's bounded static primitive checks and one genuine initial checkpoint/restore, then seals `registration.json`. It saves one fresh MJB and compares the original MJB identity separately from the adapter's compiled-array digest. The original MJB path and expected SHA are in `model-locator.json`; its payload was not read in the source flight. API compatibility, native method wrapping and physical feasibility remain unmeasured until that finite preflight.

`run` is one registered process for the fixed panel, live checkpoint, single shared retained-data traversal, conditional repeat and cross-seed restore. The active checkpoint stays alive in that process; no crash-resumability is claimed. Separate preflight/run commands share the same attempt start and counters, so process separation grants no new time or call budget. Exclusive start files prevent retrying either stage in the same root.

## Dispatch and evidence ownership

The control initial/failure state is exported through `control_evidence.control_snapshot`: every numeric controller attribute, explicit CPG configuration/state and RNG, spline coefficients/metadata and bound asset/model/input identities. It does not serialize the old object-containing checkpoint or claim portable control-native restoration. Factory creation, writer construction and initial serialization are inside the trial failure-finalization boundary.

Control is `mechanical_v16.new_body(seed, mechanical_v16.PROFILE)` followed by unchanged `mechanical_v16.step`. Its historical `CONTROL` symbol is not dispatched. Candidate starts from exactly that factory/reset, selects initial anatomical angles through `adapter.indices`, and thereafter acts solely through accepted `NativePort.prepare/apply`. The old controller is reset provenance only. The recorder copies the actual pre-step q/v immediately before the sole simulation step, then captures and commits the completed interval before any cache refresh. Runtime loops never call authoritative forward.

Core row k+1 owns physical endpoint k+1 and action/input k. Its contact group owns interval k to k+1. Candidate observation k is built from endpoint k and completed interval k-1 to k. The natural source checkpoint at poststep10000 therefore normally contains controller observation9999 and recorder interval9999. Row0 explicitly has no completed interval.

Each trial retains exclusive, float64 value/int64 tick,1000-row NumericEvidence chunks at stride1. Core has qpos73/qvel72, ctrl48, force48, input2, theta/r6 and physical/action/observation/contact clocks. State has either exact v16 fields or the candidate compact schema; no candidate legacy padding exists. Integer and Boolean encodings are validated before casting. All owned contacts, including nonfoot/nonpositive contacts, retain their index/order, ownership, signed distance, point/frame/wrench and interval endpoints. Capacity512 fails rather than truncates. Exact contact offsets and complete expected IDs are checked.

Full initial/captured native checkpoints export numeric caches and integration/scientific state. Rejected actions retain full native numeric state plus actual observation/state/task locals from the failing call. The first verification mismatch exports the reconstructed native workspace, raw rows and independently predicted task matrices/state. Compact ordinary rows avoid per-tick JSON/task/Jacobian duplication. Failure finalization aggregates returned child completeness/retention errors and required snapshot errors before publishing the trial terminal, preserving the primary exception and all secondary failures. A checked recovery manifest binds an emergency prefix, orphan chunk hashes and exact regular/emergency/initialized row counts. Emergency evidence is always incomplete. No accepted NumericEvidence code is changed.

## Independent executable verification

`verify.verify_trial(root, trial, reg, budget, model, steps)` is the complete independently implemented action and physical verification entry used once per retained trial by `run`. It consumes source q/v/contact streams, reconstructs every endpoint once with two rolling detached MjData workspaces, compares independent mesh reductions, compares object-point velocities with independently formed point-Jacobian products, and checks every active candidate action with independent intent/task/gate/hull/twist/solve/projection equations. It never calls candidate propose or accepted advance_intent as an oracle. Control explicitly uses `verify_v16` with the v16 PROFILE, not its v15 branch. A single binary verifier model is reused serially.

The callable verifier is separate from production dispatch and accepts retained files, a frozen binding, model and shared budget. It is not independently authorized to create an extra scientific traversal. `analyze` and `verify` after execution are meaningful read-only numeric entry points: they validate source stream hashes/grids/contact ownership and the single native-pass seal, recompute the original and realized physical/pose metrics, compare the saved analysis numerically, and reconstruct panel admission. The exact versioned seal requires six file hashes, four stream-start hashes, initial controller identity, true single-traversal marker and exact endpoint/contact/action coverage. Stream metadata binds trial, reset, source, preregistration and final registration. Clock, waveform, numeric domains, raw/derived grids, parent/child terminal consistency and reset/initial-state agreement are rechecked. Action checking depends on the producer attestation of the single registered traversal. Hashes bind that claim to evidence; they do not establish authenticity or replace a native reconstruction. Missing seals remain explicitly unverified; malformed seals are rejected. Any later additional native retained-data review must have its own explicit read/compute registration.

Primary theta analysis loads the exact unchanged original `_phase_window_contract`, `_cycle_metrics` and independent `_cycle_gate` function AST definitions from the immutable source. Only these named pure functions are compiled; no old runner is imported for the read-only CLI. The separate realized partition preserves actual ticks, legal boundary holds, support/reacquire events and cycle IDs. It applies the same physical median31/extrema/dwell/slip rules, with a second scalar segmentation/median/quadrature calculation. Both candidate paths must pass. Legal REACQUIRE(resume RELEASE_WAIT) to LIFT release is recognized only with the exact boundary, cycle increment, admission, distal-support dwell reset, load dwell and wait conditions. Phase conformance is separate from independent physical arithmetic agreement. Failed prefixes retain original and realized complete cycles plus clipped boundary partials only within the actual intersection of rows6000..25000; the full40000-tick denominator remains failed/inconclusive. Original helper mathematics are unchanged; only the explicit active-window indices are clipped for prefix analysis. Every panel report, including comparators, requires exact identity, completeness and validated integration/retention closure. Ordinary comparator physical failure does not itself veto candidate admission; stale original/realized/candidate aggregates do. An authored full-array positive fixture becomes a negative fixture when all feet remain loaded; neither fixture represents a physical trajectory.

## Finite schedule and resources

Development32 trials proceeds in fixed seed/case/control-candidate order. Exact whitelisted finite candidate infeasibility retains a failed prefix and allows the remaining safe development conditions. Unknown contract, arithmetic, identity, unsafe, resource or retention failures stop the attempt. Missing trials remain in all49 registered IDs. Heldout16, repeat1 and100 restoration steps remain conditional. Maximum scheduled physics is1,960,100 calls; the emergency2,600,000 bound grants no additional allowance.

`leaf-call-table.json` records the55 bounded categories, source hashes/call sites and derivations. Constructor compilation is four calls per factory: two fly neutral-keyframe builds and two world/simulation builds, at most212. The frozen original69-body metadata gives a68-fly-body upper bound; actual adapter B is validated and sealed. Candidate Ac<=638100, control Av<=308000, endpoints R<=1960149 and owned contacts E<=512N. The contactForce bound is `512*N + 512*(16*40001 + Av)`: raw completed contacts plus control row0/every endpoint plus active pre-action observations. The from_sim classmethod is charged and checks ground-contact capacity before calling its reader. Factory constructors and failure exports add no from_sim calls. The loose excursion bound is explicit, not an assumed low event rate. Source-level array/property reads and assignments stay in the frozen composition call graph; listed native editing/API methods are charged before calls. Unknown mj_/mju_ calls stop before invocation.

Wall4h includes preflight/run/reconstruction/finalization. Preflight600s includes a60s finalization reserve. RSS2GiB, all source/evidence/scratch8GiB,20GiB free floor and128MiB finalization reserve are checked. Native calls and expensive allocations are guarded; runtime checks every100 ticks and chunk flush. Numeric chunk bytes and explicit JSON/native exports are tracked incrementally, with a periodic bounded filesystem reconciliation. Fixed setup files are not extrapolated as a per-tick byte rate. Disjoint remaining setup, per-profile runtime, reconstruction, analysis/finalization and conditional repeat/restoration categories are persisted. Their twice-maximum-measured-rate costs are summed with elapsed time and at least60s/twice-measured-finalization reserve against one deadline. Unknown categories remain explicit until registered work measures them. Pending memory allocations are checked as RSS+new bytes; pending disk and already-resident buffers are separate. Numeric buffers and compression temporaries are reserved before allocation. MjData reserves the source-derived92-array allocation plus its full arena, alignment and struct/wrapper overhead before construction; native plugins are not admitted. Stream byte forecasting remains distinct from time forecasting. No warmup, repair, deletion-to-continue or scientific rerun exists.

## Source validation and review status

The final FIX1 guarded suite has77 tests, comprising30 original fixtures (two updated to the stricter report/registration protocol),29 ported independent negative/control cases, and18 targeted correction cases. The fixtures include accepted-law compound transitions with authored intent, object-shaped control serialization, initial/factory failure finalization, emergency retention, seal mutations, all-report admission, combined resource projections, pending native-data allocation, clipped-prefix metrics, sparse contact domains and source-derived ledger hashes. The saved receipt records77 passing tests, zero native calls/modules, zero production-intent calls, zero forbidden reads, one numeric thread and disabled bytecode. Synthetic seals test protocol consistency only and never constitute scientific attestation. No scientific outcome is inferred.

`FIX1.md` maps all nine correction groups to changes and regressions. `fixture-report.json`, `fixtures.txt`, `source-flight-report.json` and `source-closure.json` bind this corrected source handoff. The initial candidate remains separately frozen by the parent. All195 accepted files are still exact. FIX1 is the sole authorized source correction; a full independent architecture/quality/tests correction review triplet follows before any scientific authority.

This handoff does not self-approve source or science. Independent architecture, quality and tests reviews still follow. Remaining uncertainty is prospective native/API compatibility, actual operation costs, mechanics and downstream causal evidence; none was probed during this source-only flight. `ready=false` and the full user goal stays open.
