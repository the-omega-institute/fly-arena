# Fly Arena Goal：生命体回放与神经剧场

更新时间：2026-09-24。状态：进行中，沿用当前 active Goal，不重复创建任务。

当前状态：rounds 1–2 已接入网页，包括 3D Phenotype Lab 场景、地图注册与 observation-only 迷宫、统一 Arena 设置、科学指引、生命谱系树、凭据支持的回放检查和 `arena-geometry-v1`。Issue #82 phase-1 诊断已记录，phase-2 预注册研究运行器已可执行并保留短程 smoke 证据；完整五种子长程研究仍按协议运行。NyxID OIDC 登录与同源 Pages 入口已由 PR #84 接入，实际部署是否启用由 `/auth/config` 报告。

## 1. 最终目标

用真实果蝇神经连接组，让人和 AI 修改神经网络参数、选择模型与演化策略、设计数字果蝇，并在三维环境中竞争和演化。

用户必须能看到自己设计的果蝇如何感知、行动、找到并摄取食物、回应障碍和对手；同屏检查这份大脑在当时的实际活动、连接和修改。每一代都是可回看的电子生命样本。

完成标准是一条用户能独立走通的路径：

**了解项目 → 设计自己的大脑 → 与 WT 同条件预览 → 三维比赛与同步脑图 → 选择算法演化 → 观察每代行为 → 保存后代 → 与 WT、亲代或其他用户的设计再次竞争。**

## 2. 用户应该看到什么

- **三维生命剧场**：可旋转、跟随、暂停和慢放。身体具有实际姿态和足部运动，场景包含有限食物、地形、障碍和对手。显示接触、摄取、碰撞、倒置等真实事件。
- **当前个体的大脑**：整体结构 → 功能群/神经元类别 → 单神经元及局部连接。查看基线权重、设计倍率、实际活动曲线；切换个体时身体和脑图一起切换。
- **同一条时间轴**：身体、感觉输入、神经活动、运动指令、资源变化同步。点击“接触食物”即可检查事件前后哪些已记录神经元发生变化，随后是否真正摄取。
- **可操作的设计工坊**：简单回路滑块与高级权重编辑共用 FlySpec；显示修改预算、亲代来源和预览入口。可从回放中的大脑创建自己的后代草稿。
- **演化工作台**：选择兼容的神经动力学模型、学习规则和优化算法；提交自定义策略；查看预算、排队状态、逐代个体、失败记录和后代回放。
- **首次使用与 AI 指引**：解释连接组来源、仿真如何工作、哪些是实验假设；提供 AI 可直接使用的设计样例、提交和评估说明。

脑图亮度必须来自该个体的实际仿真记录。整体结构可以完整展示，活动采样范围必须明确；未记录不填零，真实零值保留。没有解剖坐标的神经元通过局部连接图和曲线查看，不编造空间位置。

## 3. 实现架构

```text
网页设计 / AI / 自定义优化策略
                ↓
     FlySpec：连接组引用、权重修改、神经参数
                ↓
     格式、兼容性与 mutation budget 校验
                ↓
环境 → 感觉编码 → 连接组动力学 → 动作解码 → 身体与物理环境
 ↑________________________________________________↓
                ↓
 同步记录：姿态、输入、活动、动作、事件、资源
                ↓
 三维生命剧场 + 大脑观察 + 对照评估 + 生命谱系
                ↑
 优化算法根据评估结果生成下一代 FlySpec
```

继续使用 Python/Numba 神经计算、MuJoCo 身体与物理、Three.js 网页展示。先贯通现有架构；只有性能分析证明确有热点，再局部引入 C++/CUDA。

canonical connectome 保持原始结构；用户修改编译为设计权重，例如 `W = W0 × m`。预算可采用 `Σ cᵢⱼ |log mᵢⱼ| ≤ B`，最终按比赛规则固定并在页面解释，避免靠无限放大权重取胜。网页和 AI 使用同一套校验。

神经动力学、个体内可塑性与跨代优化是三个独立选择，界面只提供有效组合。先复用已经实现的算法并展示真实样本，再按支持情况扩展 CEM、遗传搜索、梯度/代理梯度等策略；不能把算法名称可选当成算法已完成。

自定义策略通过受资源限制的独立进程或沙箱生成候选，调用同一个评估入口；明确评估次数、超时、并发和结果格式。优化器不能自行改写比赛得分。

NyxID 负责平台身份、权限和配额，预留一次授权的登录适配接口；正式接入等用户后续安排。Ornn 可提供 AI 设计技能。两者均独立于神经计算、物理和评分。

## 4. 实施顺序与验收

| 优先级 | 交付 | 用户验收 |
| --- | --- | --- |
| P0 | 可观察的觅食生命剧场 | 打开实际回放，从探索、接近、接触看到摄取；事件定位同时更新身体、感觉、大脑和资源；失败过程也可看 |
| P0 | 神经观察入口贯通 | 点选味觉/触觉群，进入实际神经元、连接权重和曲线；无坐标节点仍然可查看；未采样不伪造活动 |
| P0 | 网页完整闭环上线 | 新设计可保存、预览、参赛、演化、保存后代并再次参赛；演示样本和用户任务明确区分；真实浏览器检查三维画面与交互 |
| P1 | 设计与对照 | 相同刺激和条件下比较自己的设计与 WT，显示神经与动作差异；从比赛大脑创建后代 |
| P1 | 有实际交互的竞技 | 双蝇争夺同一份有限食物，双方身体和脑图均可检查；支持 WT/公开设计、换位对照和结果回看 |
| P1 | 多算法演化与自定义策略 | 展示数种实际运行的演化轨迹；用户选择算法或提交策略，每代可看行为、修改、预算和失败原因 |
| P1 | 复杂三维地图 | 在现有场景上加入有效的障碍、高差、资源分布与感觉变化；地图确实影响身体和感知，之后扩展攻击/逃避赛制 |
| P1 | 电子生命账本与贡献入口 | 从后代追溯亲代、修改、算法、评估条件和原始回放；人和 AI 共用提交接口，首次使用有完整指引 |

先把已有能力接成能用的产品，再增加地图和算法。每次交付提供页面入口、可见变化、操作路径及剩余缺口。

## 5. 当前已有成果和实际缺口

以下是本地交接与研究文件所记录的状态，不能代替新的远端或在线检查。

- 已有完整连接组计算、实际 MuJoCo 身体回放、WT 对照、共享资源竞争、演化样本、谱系记录、解剖脑图与单神经元曲线。PR #68–#70 的交接记录为已合并。
- 最新 15 秒感觉神经元样本已生成并通过 HTTP 数据检查：1,501 帧、66 个采样节点；摄取 20 单位，后半段 10 单位，未记录倒置。新增采样保留了旧记录相同时间范围内的身体、感觉、动作和原有 48 个节点活动。这是记录能力扩展，不是新一次优化成功。
- 实际样本中味觉节点 23209 在 3.65 秒约为 34.06 Hz，右触觉节点 802939 在 11.27 秒约为 33.89 Hz；该段采样的左触觉节点全部为零，按实展示。
- PR #71 已通过 CI、合并并发布到现有本地页面，HTTP 核对通过。没有源数据胞体坐标的触觉群会从解剖视图进入局部连接图；真实数据交互已验证零值、峰值定位和设计权重，浏览器视觉效果仍待验收。
- 新后端在线接通、完整网页流程、真实浏览器/WebGL 视觉验收仍未完成。静态样本可读、测试通过和源码合并分别记录，不能合并声称产品已上线。
- 上述样本实际由本机 CPU 运行；没有证据表明已完成 4060 计算或渲染。远端是否可访问需要实际检查，不能据此判定节点离线。

## 6. 接下来直接执行

1. 已完成感觉群到实际神经元的查看路径。接下来让 WT 与固定演化后代同场争夺有限食物，记录双方神经响应，并把逐食物点摄取接到身体和大脑观察入口。
2. 完成相关前端检查与当前 PR 的 CI，正常合并、同步并发布可用增量。
3. 接通新后端与网页设计、预览、训练、保存后代和再次参赛；在真实浏览器中走完整条路径，处理三维展示和新用户指引的问题。
4. 优先改善持续身体行为和共享食物竞争，再以不同条件评估固定设计；计算按资源预算串行排队。
5. 基于同一评估入口展示多算法演化样本，贯通自定义策略、谱系和 AI 提交。

## 7. 科学记录与工程原则

- 真实连接组约束结构；神经动力学、感觉编码、动作读出仍是模型。数字果蝇不能宣称等同真实生物。WT 表示基线模型对照。
- 同步活动只说明观察到的相关变化。声称某回路导致行为时，需要同条件扰动或消融证据。
- 保留设计/亲代、数据与模型版本、算法配置、地图、种子、感觉/读出条件、行为指标和回放。训练与独立评估分开，成功与失败都入账。
- 资源守恒、神经输入与记录一致、缺失数据处理、关键用户流程和构建纳入有意义的 CI；不增加多轮审批或逐文件哈希流程。
- 可访问时优先将适合的长计算排队到 4060；Mac Studio 承担适合其能力的服务与计算。记录实际执行节点、耗时和产物。网页渲染、离线渲染与神经计算分别说明。
- 已完成实验复用结果；每个可用增量使用正常 PR，CI 通过后及时合并。部署和浏览器验收单独确认，不以接口数量或实验次数充当用户成果。

## 8. 文件与现有样本

- [详细方案与历史进度](docs/GOAL_NEURAL_LIFE_THEATER.md)
- [最新感觉神经元样本记录](var/research/sensory-neuron-replay/publication.json)
- [实际 HTTP 数据检查](var/research/sensory-neuron-replay/http-publication.json)
- [最新样本回放](http://127.0.0.1:8080/#tab=arena&match=69996bd682d247449b2c75def3cb3528)
- [已运行的演化画廊](http://127.0.0.1:8080/#tab=train&showcase=da3453d7f05e4f25aa1317037fe87fb9)

本地回放链接依赖已有 8080 服务，本次整理未重新检查服务或上线情况。研究产物位于本地 `var/`，不保证随 Git 仓库分发。

**Goal 只有在上述用户闭环可用、能观察真实身体与神经记录、能运行并比较演化后代后才算完成；写好本文不代表完成。**

## 2026-09-20 网页闭环验收增量

- 新后端已在 http://127.0.0.1:8081 运行，使用独立数据库；真实浏览器完成嗅觉倍率 1.05 的设计、本人设计与 WT 的神经预览、保存与进入训练。
- 实验沙箱比赛保留训练所选感觉—运动方案，兼容的 research-v2 后代可以与 WT/公开设计参赛；双循环交换出生位置，实验结果不进入正式排行榜。
- 训练支持最长 30 秒，网页默认 5 秒；已从真实网页提交 2 代 × 2 个个体 × 20 秒的 CEM 持续觅食实验。结果和后续后代比赛尚待完成验收。
- Mac Studio 常驻服务和 4060 SSH 已确认在线；正在同步当前源码、模型读出与可观看样本。计算执行与图形渲染分别记录，不将 GPU 在线等同于 CUDA 神经训练。

## 2026-09-20 实际交付：设计、演化、对照与神经观察

- PR #73 已通过 CI 并合并；v0.7.3 已发布并部署到 Mac Studio 常驻服务。本地可用入口为 http://127.0.0.1:8081 。
- 实际浏览器完成新设计（嗅觉 ×1.05）、与 WT 神经预览、保存、提交 CEM 训练、选择第二代后代、保存并与 WT 发起新比赛；没有以预制分数代替计算。
- CEM 实验 `a7dd672768234a90938649e7276dcc1d`：2 代 × 2 个体 × 20 秒，种子 42。第一代最佳摄食 8.0624、后半程 0；第二代后代 `5eb2221e6be248d4bce6e058547f863e` 摄食 17.9808、后半程 7.9808、持续觅食目标值 25.9616。四次评估均保留，零摄食的个体也可查看。
- 保存后代与 WT 的比赛 `ed14dcbcca3542769646cc0884746ab3`：相同感觉与运动方案，种子 43、10 秒。后代摄食 16.2672，WT 1.4456；均全程正立。这是单种子、单出生位置的结果，不代表多种环境中的普遍优势。
- 身体与解剖脑图并排同步显示，实际浏览器确认两张 WebGL 画布正常。已有触觉记录支持从零活动定位到 11.27 秒 33.89 Hz 峰值，同时移动身体时间轴。解剖背景为 140,024 个实际胞体坐标，前景显示记录节点；未采样区域不伪造活动。
- 已发布 CEM、evolution、random search 的实际轨迹；短窗口 random search 是流程样本，不能用来宣称训练有效。自定义算法沿现有外部优化器接口提交候选、评估并取回结果。
- Mac Studio 已更新源码、网页、研究读出和公开样本；不复制私有账户。RTX 4060 已实际完成地图渲染，完整比赛视频继续在独占队列中渲染。神经评估使用 CPU/Numba，未宣称 CUDA 训练。
- 远端复制的独立比赛回放也应进入生命经历和观测摘要，避免样本能播放却在履历中丢失。

入口：
- [新 CEM 演化轨迹](http://127.0.0.1:8081/#tab=train&showcase=a7dd672768234a90938649e7276dcc1d)
- [后代持续觅食](http://127.0.0.1:8081/#tab=arena&match=4b47bd8687ad49d1afed1b16f1d443ce)
- [后代与 WT 对比](http://127.0.0.1:8081/#tab=arena&match=ed14dcbcca3542769646cc0884746ab3)
- [后代生命档案](http://127.0.0.1:8081/#tab=life&fly=5eb2221e6be248d4bce6e058547f863e)
- [可部署产物与公开研究样本](https://github.com/the-omega-institute/fly-arena/releases/tag/v0.7.3)

## 2026-09-20 用户参考图纠正：真实纤维形态

用户提供的参考图要求可辨识的神经纤维、分叉和空间结构。此前“胞体点云 + 采样节点发亮”只完成了基础观测闭环，不能视为已经达到该可视化目标。

已确认 MaleCNS 官方公开 SWC 骨架，body ID 与现有仿真一致。新增形态导入和真实纤维渲染，将记录活动映射到对应神经元的真实骨架；结构配色、实际活动、未采样和选中高亮分别说明。首批 232 个神经元、723,390 段真实分支覆盖当前回放全部 66 个记录节点。见 [形态数据来源、当前实现与剩余工作](docs/NEURAL_MORPHOLOGY.md)。

全脑随时间变化的活动展示仍需要新运行的密集记录和流式加载。旧回放没有记录的神经元不能靠增加形态或插值补成“全脑活动”。当前单室神经模型也没有模拟纤维内的传播，不制作虚假的沿轴突跑动光效。
