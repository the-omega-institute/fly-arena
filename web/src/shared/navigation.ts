export type Tab='design'|'arena'|'lab'|'train'|'code'|'life'
export function readRoute(hash:string){const p=new URLSearchParams(hash.replace(/^#/,''));const experiment=p.get('experiment')||'';const match=p.get('match')||'';const series=p.get('series')||'';const value=p.get('tab');const tab:Tab=experiment?'lab':match||series?'arena':value==='arena'||value==='lab'||value==='train'||value==='code'||value==='life'?value:'design';return {tab,experiment,match,series}}
export function routeHash(tab:Tab,experiment='',match='',series=''){const p=new URLSearchParams({tab});if(tab==='lab'&&experiment)p.set('experiment',experiment);if(tab==='arena'&&match)p.set('match',match);if(tab==='arena'&&series)p.set('series',series);return '#'+p.toString()}
