# 本地 agents 执行约定

## 目标

交付地图编辑、共同权威世界、参赛接口、双蝇对抗、真实状态观战和回放。用户已经明确：玩家自带控制器，平台不开发大脑编辑器或自有脑模型。测试 controller 只是平台验证工具，不是参赛算法研究任务。

## 规范与优先级

用户明确指令优先。产品/安全边界以 `docs/IMPLEMENTATION.md` 为准；实验方法和结果口径以 `docs/EXPERIMENTS.md` 为准；样本数、seed 和运行条件以 `experiments/plan.json` 为准。发生矛盾先修正文档并标注变更，不能自行选取较宽松标准。

计划是设计冻结前的 draft。通过预检及身体驱动校准后，维护者审核运行配置、精确依赖、资产哈希和阈值并冻结；确认批次之前保存 git SHA 和计划哈希。结果出现后不得原地修改已冻结方案。任何调参都需要新版本与未使用的 seed。

## 分工

| Agent | 负责 | 优先实验 | 交付 |
|---|---|---|---|
| A | profiles、协议、Python SDK、测试 controller | E01/E04 | 两个独立进程通过同一接口参赛 |
| B | Three.js 地图、编辑器、状态播放器、回放 | E01/E05/E10 | 页面消费 worker 实际姿态，无伪造动画 |
| C | FlyGym 适配、两身体同世界、公共驱动、裁判 | E00/E02/E03/E08 | 无头真实接触对局、事件和回放 |
| D | API、认证、PG 任务租约、云端/本地部署 | E00/E04/E07 | 出站连接、故障隔离与持久化 |
| E | 独立验收、排程、指标、失败复现 | E05-E11 | 报告及哈希关联的原始证据 |

A/C 先冻结接口并验证双蝇接触；B/D 可以使用显式 fixture 并行开发。集成验收必须替换为真实运行数据。E09 GPU 和 E11 扩展锦标赛不阻塞通过 CPU 路径的最小版本。

## 建议代码目录（需要实现）

`apps/web/`、`apps/api/`、`packages/contracts/`、`packages/sdk-python/`、`services/sim-worker/`、`deploy/`。本 PR 只提供设计和排程工具，不声称这些应用已存在。

先交付单世界 CLI 和两个参赛示例，再接本地 API 与网页，再部署远端。实现后的 CLI 必须用 `--help` 和实际运行证据验证。不得仅根据文档中尚未实现的命令宣称服务已启动。

## 不可跳过的约束

- 一场比赛只在一个物理世界内求解，碰撞和伤害由平台计算。
- 玩家只能返回动作，不能设置 qpos、坐标、HP、死亡或胜者。
- 不执行陌生人镜像、Python、MJCF 或任意地图脚本；不访问玩家提供的回调 URL。
- 不在每个物理步访问公网，不用 wall-clock 等待增加模拟年龄。
- 4090 主机不开放入站服务，不挂载 Docker socket、研究数据或私人 SSH key 给 worker。
- 不擅自重装驱动、重启现有 Docker、改防火墙、删除容器或迁移用户数据。
- 用整数 physics step 处理命中、冷却、终止与采样；同时结算双方伤害。
- 仿真错误、中断、技术判负与游戏死亡分开记录。
- GPU 的求解器选项和批量调度另做验收；不能静默替代 CPU 或宣称等价。
- fixtures、dry-run、静态检查只证明各自范围。`PASS` 必须附命令、环境、原始证据与哈希。

## 结果写回

每次实现提交附对应实验 ID。报告使用 `experiments/RUN_REPORT.md`；原始大文件不直接提交 Git，提交清单、SHA-256 和受控存储定位。不得上传 token、访问日志中的凭据、个人信息或研究数据。

本任务授权在 fly-arena 仓库提交实施成果的分支/PR；不要直接合并、强推 main 或更改仓库可见性。无需为通过 GitHub CI 而改变科学和测试口径，本地真实执行证据必须明确。
